# Dispatcher cycle — 2026-07-04 02:11 UTC (caro-merge-review-integrate)

## Forward-path classifications (this cycle)

37 open, non-draft, user-authored PRs classified (bots excluded). Path labels
carried from prior cycles where present; all path:auto/scoped/deep PRs lacking
an in-flight rebase bead were dispatched, mergeable-first, capped at 15/cycle.

| Path | Count | Notes |
|---|---|---|
| path:auto | 11 | mechanical rebase; P2 |
| path:scoped | 16 | targeted hand-edit; P3 |
| path:deep | 8 | structural/backend; P1, claim_policy:manual_only |
| path:refuse-list | 4 | #1246, #1061, #1043, #1004 — policy-gated, no bead (correct) |

## Newly filed beads (this cycle, capped at 15)

Parent epic: caro-jac (v1.2.0 PR Rebase Queue).

- caro-jac.29 — gh-1212-rebase — path:deep P1 (MERGEABLE)
- caro-jac.32 — gh-1166-rebase — path:auto P2 (MERGEABLE)
- caro-jac.35 — gh-1156-rebase — path:scoped P3 (MERGEABLE)
- caro-jac.36 — gh-1155-rebase — path:scoped P3 (MERGEABLE)
- caro-jac.37 — gh-1124-rebase — path:scoped P3 (MERGEABLE)
- caro-jac.38 — gh-1088-rebase — path:deep P1 (CONFLICTING)
- caro-jac.39 — gh-1042-rebase — path:deep P1 (CONFLICTING)
- caro-jac.40 — gh-993-rebase — path:deep P1 (CONFLICTING)
- + gh-1245/1244/1228/1199/1187/1159/1158-rebase — path:scoped P3 (MERGEABLE)
  (inserted OK; bd read-view stale this cycle — see coordination alert)

## Dispatch cap

Cap hit (~15/cycle). Deferred to next cycle (CONFLICTING, lower signal):
#1184 (scoped), #1153 (scoped), #1071 (scoped), #886 (auto),
#838 (deep), #808 (deep), #805 (deep).

## Coordination alerts (to Hermes)

- **Beads DB read-view inconsistency**: `bd create` inserts succeed (UNIQUE
  constraint on external_ref confirms rows exist) but `bd list`/`bd show`
  return a stale snapshot missing 7 of this cycle's beads. Consistent with the
  prior Dolt/DB-corruption incident; corrupt DB artifacts present in worktree.
  Recommend a daemon restart / DB reconcile before next cycle to avoid
  idempotency false-negatives.
- **2 open release-gap P0/P1 issues** would BLOCK any /caro.release.prepare run
  per release-acceptance-verification gate. Must be closed/waived before the
  [Unreleased] block can graduate.

## Release-readiness gates

- CHANGELOG [Unreleased] items: 4 (threshold ≥3 MET)
- Current shipped version (Cargo.toml): 1.4.0
- Threshold met?: YES — but BLOCKED by 2 open release-gap P0/P1 issues.
  Human/Hermes to decide target version line and clear gaps before prepare.
