# Dispatcher cycle — 2026-07-07 02:11 UTC (caro-merge-review-integrate)

## Summary

37 open, non-draft, user-authored PRs classified. **Zero new beads filed** —
every dispatchable PR already carries ≥1 open `gh-<N>-rebase` bead, so the
idempotency guard (query external_ref across ALL statuses) correctly held.

## Forward-path classifications (this cycle)

| path | count | PRs |
|---|---|---|
| path:auto | 12 | 1280, 1261, 1224, 1196, 1194, 1178, 1175, 1173, 1171, 1167, 1166, 886 |
| path:scoped | 15 | 1245, 1244, 1228, 1199, 1187, 1184, 1159, 1158, 1157, 1156, 1155, 1153, 1124, 1071 (+1042 mislabel, see alert) |
| path:deep | 5 | 1212, 1088, 993, 838, 808, 805 |
| path:refuse-list | 5 | 1246, 1061, 1043, 1042, 1004 |

(Conflicting-at-HEAD: 1157, 1153, 1088, 1071, 1061, 1042, 1004, 993, 886, 838, 808, 805.)

## Newly filed beads
- (none) — all 32 dispatchable PRs already have in-flight rebase beads.

## 🚨 Coordination alert — rebase-bead explosion (ownership decision needed)

The `gh-<N>-rebase` external_ref namespace has re-accumulated duplicates.
Two dispatchers own these refs with different ID schemes and neither can
dedup the other via the embedded-Dolt guard (no UNIQUE(external_ref)):

- **caro-jac.\*** — resurfacing queue (claim_policy:manual_only)
- **caro-XXXX** — merge-integrate dispatcher (this routine)

Current state (open beads only):
- **141** open `gh-*-rebase` beads across **42** distinct refs
- **24** refs carry duplicates; **99** excess beads (target is 1/ref)
- Worst offenders: gh-1158, gh-1156, gh-1155, gh-1124, gh-1071 — **8 beads each**

This is a recurrence of the 2026-06-26 explosion (bd memory
`groom-rebase-bead-explosion`). Per that memory, dedup ownership is a
**policy decision** — this dispatcher does NOT unilaterally close 99 beads.
Requesting Hermes route an ownership/dedup decision: pick the canonical
scheme (recommend keeping the lowest-numbered `caro-jac.*` per ref, closing
the rest as `duplicate-of`), OR collapse the two dispatchers onto one ref
scheme so the guard actually fires.

## Classification discrepancy (for the record)
- **PR #1042** is labeled `path:refuse-list` but touches only Tier-2
  `src/backends/**` (auto_rebase: true) — policy says this is
  `path:scoped` (rebase-only), not a hard refuse. No decision comment
  posted (a refuse-list rationale would be inaccurate). Existing bead
  `gh-1042-rebase` already covers it. Flagging rather than silently
  relabeling.

## Release-readiness gate
- CHANGELOG [Unreleased] items: **4** (threshold ≥3 **MET**)
- Current shipped version (Cargo.toml): **1.4.0**
- Recommendation: a human (or Hermes) should decide which version line the
  4 accumulated `[Unreleased]` items belong to and whether to run
  `/caro.release.prepare`. Dispatcher does not guess the target version.
