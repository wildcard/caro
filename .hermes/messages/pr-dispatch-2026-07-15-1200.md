# Dispatcher cycle — 2026-07-15 12:00 UTC (caro-merge-review-integrate)

## Forward-path classifications (this cycle)

42 open, non-draft, user-authored PRs. All 42 carry exactly one `path:*`
label; no unlabeled PRs and no double-labels. Distribution:

| path | count | meaning |
|---|---|---|
| `path:auto` | 16 | mechanical rebase — coder-loop claimable |
| `path:scoped` | 12 | targeted hand-edits, content valid |
| `path:deep` | 9 | structural — `claim_policy:manual_only` |
| `path:refuse-list` | 5 | policy-blocked, no bead by design |

Steady-state cycle: the classification set was already complete from prior
cycles. No PR required a new or changed path label.

## Newly filed beads

**None.** 37 of 42 PRs already carry a `gh-<N>-rebase` bead; the remaining 5
are `path:refuse-list` and are not beadable by contract (they wait on policy
adjustment, not coder work).

Idempotency guard confirmed working: `bd list --limit 3000` over all statuses
returned 89 `gh-*` refs, and the open-PR diff produced exactly 5 candidates —
all of which correctly resolved to refuse-list. Zero duplicate filing.

## Refuse-list verdicts re-verified against policy.yaml

All 5 were re-checked against `.claude/automation/config/policy.yaml` rather
than trusting the prior cycle's labels. Every one touches
`.github/workflows/**`, a Tier 1 hard-refuse glob
(`auto_dispatch: false`, `auto_rebase: false` — CI mutation has cross-branch
blast radius; STAKEHOLDERS assigns @wildcard human-only).

| PR | title | refuse driver |
|---|---|---|
| #1004 | fix(ci): validate-translations.yml artifact paths + flaky cache_contract test | `.github/workflows/validate-translations.yml` |
| #1043 | fix(windows): repair stdin hang, POSIX leak, shell mis-detection | `.github/workflows/ci.yml`, `release.yml` |
| #1061 | fix(ci): mark ChromaDB Integration Tests non-blocking | `.github/workflows/ci.yml` |
| #1308 | feat(flox): Flox use case, dev env, isolation rule, packaging | `.github/workflows/packages.yml` |
| #1314 | feat: multi-harness learnings from Warp — best-of-N ranking | `.github/workflows/automation-scheduler.yml` |

No new decision-frame comments posted — all 5 already carry the
`path:refuse-list` label from a prior cycle, and the verdict is unchanged. A
repeat comment would be noise on an unchanged decision.

**Standing observation for Hermes:** 5 of 5 undispatched PRs are blocked by a
single glob. #1061 in particular is a size/XS, 2-file PR whose entire purpose
is to unblock the merge queue, and it is itself blocked by the workflow-glob
refuse. This is the highest-leverage policy question in the queue: whether a
narrower constraint (e.g. a `ci_non_blocking_only` sub-rule) should let
small, non-release CI edits through. Policy edits are out of this routine's
scope — surfacing for a human decision.

## Coordination alerts (to Hermes)

- (none) — no parallel-branch conflicts detected against existing rebase beads.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4**
- Current shipped version (Cargo.toml): **1.4.0**
- Threshold met (≥3 unreleased items)?: **YES**

The gate is met. This routine does not guess the active release-tracking
issue, so no GitHub comment was posted. A human (or Hermes) should decide
which version line the `[Unreleased]` block belongs to (1.4.1 vs 1.5.0) and
whether to run `/caro.release.prepare`.
