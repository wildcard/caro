# Dispatcher cycle — 2026-09-24 07:00 UTC (caro-merge-review-integrate)

Cycle outcome: **near steady state**. 72 open user-authored non-draft PRs,
70 already classified by prior cycles. 2 newly classified, 1 bead filed,
1 refuse-list decision comment posted. Dispatch cap (~15/cycle) NOT hit.

## Forward-path classifications (this cycle)

| PR | path | owner | bead filed | notes |
|---|---|---|---|---|
| #1470 | `path:refuse-list` | wildcard | none (correct) | 3 globs fire: `.github/workflows/**` + `src/execution/executor.rs` (both hard-refuse), and `Cargo.toml` **adds** `libc = "0.2"` under `[target.'cfg(unix)'.dependencies]`, violating the `deps_only` constraint. Decision-frame comment posted. |
| #1459 | `path:scoped` | wildcard | `caro-jac.112` | Rebase-only. 41 files / size/XL. No hard-refuse glob, but `src/backends/**`, `src/prompts/**`, `src/agent/mod.rs`, `src/main.rs` are all `auto_dispatch:false` — coder-loop may rebase, must not edit content. |

## Newly filed beads

- `caro-jac.112` — `gh-1459-rebase` — `pr-dispatch,loop:rebase,path:scoped,dispatched-by:merge-integrate` (P3, parent `caro-jac`)

## Idempotency check

- `bd list --limit 3000` returned **285** beads — no truncation (the 50-row
  default that caused the 2026-06/07 99-bead explosion was not in play).
- 61 of 72 open PRs already carry a `gh-<N>-rebase` bead.
- The 11 without a bead: 9 are `path:refuse-list` (correctly unbeaded — they
  wait on policy/human, not coder capacity), plus the 2 processed above.
- Zero duplicate beads filed. Zero PRs carry more than one `path:*` label.

## Backlog shape (all 72 open PRs)

| path | count |
|---|---|
| `path:auto` | 35 |
| `path:scoped` | 18 |
| `path:refuse-list` | 10 |
| `path:deep` | 9 |

35 `path:auto` PRs — mostly `chore(qa): scheduled rotation` and weekly
planning reports, size/XS, MERGEABLE. This is the drainable head of the
queue and it is growing faster than it is being consumed. The QA-rotation
PRs in particular (#1453, #1445, #1443, #1441, #1389, #1373, #1371, #1367,
#1363, #1333, #1321, #1299, #1224, #1178, #1175, #1171, #1167, #1166, and
older) are near-identical doc-only additions accumulating since 2026-05.
**Suggest Hermes flag for a human decision: batch-merge or batch-close
the QA-rotation backlog.** Filing 20 individual rebase beads for
mechanically identical doc PRs is queue noise, not throughput.

## Coordination alerts (to Hermes)

- **Security Audit is red repo-wide.** Both newly-processed PRs (#1459,
  #1470) fail `Security Audit` / `cargo-audit`. Neither introduces the
  failure — it is the standing advisory backlog tracked by open PR #1311
  (`fix(security): clear all Security Audit advisories`, `path:deep`,
  bead `caro-jac.80`, CONFLICTING). Every PR's merge is gated behind
  #1311 landing. This is the single highest-leverage unblock in the
  queue and it is itself conflicted. Recommend escalating #1311 to a
  named delivery agent.
- **Stale git lock in the main working tree.** `.git/index.lock` exists,
  zero bytes, mtime `2026-07-23 02:36:31` — two months old, with no live
  `git` process holding it. Any `git commit` from the main repo root will
  fail with "Another git process seems to be running". This did not block
  bead filing (the bd daemon writes its own store), but it will block the
  next agent that tries to commit from `/Users/kobik-private/workspace/caro`
  rather than a worktree. **Not removed** — deletion requires explicit
  human confirmation per `~/.claude/rules/destructive-commands.md`.
  Remedy once confirmed: `rm /Users/kobik-private/workspace/caro/.git/index.lock`.
- `bd sync` remains a non-existent command (`unknown command "sync" for "bd"`,
  bd v1.0.4). Already tracked by `caro-gr3p`; no new bead filed. The daemon
  auto-exports to `.beads/issues.jsonl`, so `git add .beads/` is the correct
  flow. Three SKILL/command surfaces still carry the stale instruction.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4**
- Current version (`Cargo.toml`): **1.4.0**
- Threshold met (>= 3 unreleased items)?: **YES**

The `[Unreleased]` block's headline item is runtime-loadable custom safety
patterns via TOML (additive on top of the built-in pattern database, with
`risk_level` capped at High and Critical built-ins non-bypassable). A human
should decide which version line this belongs to before
`/caro.release.prepare` runs. Note PR #1432 is open specifically to
disambiguate published 1.4.0 vs in-repo 1.5.0 — resolve that first, since
the release-version-alignment 6-file checklist keys off the answer.
No GitHub comment was posted to any epic: this routine does not know the
active release-tracking issue and must not guess one.
