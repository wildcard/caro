# Dispatcher cycle — 2026-05-15 02:11 UTC (caro-merge-review-integrate)

**Cycle ID**: head 62d293fb · 2026-05-15T02:11:11Z
**Routine**: `~/.claude/scheduled-tasks/caro-merge-review-integrate/SKILL.md`
**Working tree**: `feat/openrouter-backend` (dispatcher runs are read-only against milestone PRs; no commit needed)

## Forward-path classifications (this cycle)

All 9 open v1.2.0 PRs already carry `path:*` classifications and (where eligible) rebase beads from the 2026-05-13 cycle. Idempotency check confirmed — no new beads filed.

| PR | path | mergeable | rebase bead | notes |
|---|---|---|---|---|
| #680 | path:scoped | CONFLICTING | caro-jac.3 (open) | i18n automation scripts — clean rebase scope |
| #652 | path:scoped | CONFLICTING | caro-jac.4 (open) | DCO sign-off requirements |
| #651 | path:auto | UNKNOWN | caro-jac.1 (open) | reusable prompt — docs only |
| #609 | path:auto | UNKNOWN | caro-jac.2 (open) | install.sh CLI flags |
| #605 | path:scoped | CONFLICTING | caro-jac.8 (open) | **policy interaction**: adds `libc = "0.2"` to Cargo.toml (deps_only constraint), src/main.rs touch (no_new_flags). Soft-classified pending policy ratification. |
| #599 | path:deep | MERGEABLE | caro-jac.9 (in_progress, claim_policy:manual_only) | Astro DB + Turso waitlist — claimed by caro-waitlist-engineer |
| #597 | path:scoped | CONFLICTING | caro-jac.7 (open) | slash commands + reference docs |
| #574 | path:scoped | CONFLICTING | caro-jac.6 (open) | **policy interaction**: install.ps1 Tier 3 distribution surface (auto_rebase:true, auto_dispatch:false). Bead notes rebase-only constraint. |
| #128 | path:scoped | CONFLICTING | caro-jac.5 (open) | deps skill + skills guide |

**Verdict counts**: 2× path:auto, 6× path:scoped, 1× path:deep, 0× path:refuse-list, 0× path:decision.

## Newly filed beads

**None** — all 6 candidate beads (caro-jac.3 through .8) already exist in JSONL from the prior cycle (2026-05-13). The `bd create --external-ref` UNIQUE constraint correctly blocked re-filing. Dispatcher idempotency contract is intact across cycles.

## Coordination alerts

### 🔴 P1 — Local SQLite DB out of sync with `.beads/issues.jsonl`

- `bd list` returns 3 rebase beads (caro-jac.1, .2, .9)
- `.beads/issues.jsonl` contains 6 additional rebase beads (caro-jac.3 through .8)
- The JSONL is the canonical source — UNIQUE constraints fired against it, not against `bd list` output
- Session-start git status flagged: `M .beads/issues.jsonl`, untracked `.beads/.sync.lock`, `.beads/sync-state.json`, `.beads/sync_base.jsonl`, `.beads/beads.db-shm`, `.beads/beads.db-wal` — signs of an interrupted sync
- `bd sync --status` errors: `sync.branch not configured`

**Impact**: future dispatcher cycles relying on `bd list` for the idempotency check will believe beads are absent and attempt to re-file. The UNIQUE constraint will continue to block duplicates, but the cycle will spam errors and waste cron budget.

**Suggested fix-up (out of scope for dispatcher)**: someone needs to run `bd config set sync.branch <branch>` on a clean checkout and let `bd` rebuild the SQLite from JSONL.

### 🟡 P2 — Dispatcher reframe PR #1087 is OPEN + CONFLICTING

- The `policy.yaml` schema that Phase 2 of this routine depends on lives at `.worktrees/dispatcher-reframe/.claude/automation/config/policy.yaml`
- It is NOT yet present on `main` — only in PR #1087 (branch `feat/dispatcher-reframe`)
- Prior cycle (2026-05-13) and this cycle both read it from the worktree to make classifications
- PR #1087 itself is conflicting against main

**Impact**: the policy file is in a Schrödinger state — load-bearing for this routine but not yet ratified. Once #1087 lands, soft-classified PRs (#605, #574) may need reclassification under strict policy reading.

**Suggested action**: rebase + merge PR #1087 to canonicalize the dispatcher schema.

## Refuse-list deferrals (soft → strict policy gap)

Under a strict reading of the worktree `policy.yaml`:

| PR | Current label | Strict-policy label | Policy trigger |
|---|---|---|---|
| #605 | path:scoped | path:refuse-list | `Cargo.toml` adds `libc = "0.2"` — violates `deps_only` constraint (add/remove dep refuses) |
| #574 | path:scoped | path:scoped (rebase-only) | `install.ps1` net-new — Tier 3 distribution; auto_rebase:true so path:scoped is correct, but coder-loop must not edit install.ps1 |

Prior cycle chose soft classification (`path:scoped` with informational bead notes) because policy.yaml is not yet on main. This cycle preserves that decision to avoid re-classification churn. Once #1087 lands, run a one-shot dispatcher cycle in strict mode to upgrade labels and post `[agent]` decision comments where appropriate.

## Release-readiness gates

- **CHANGELOG.md `[Unreleased]` entries**: 2  (threshold: ≥3)
- **Milestone v1.2.0 progress**: 7 closed / 37 total = **18%** (threshold: ≥80%)
- **Threshold met**: **NO**

No `/caro.release.prepare` suggestion this cycle. The milestone is still in mid-flight; the dispatch beads above represent the bulk of remaining work.

## Phase 4 authorship guard

No auto-merge attempted this cycle. The guard only fires when `path:auto` PRs are candidates for `gh pr merge` after coder-loop rebase. Both `path:auto` PRs (#609, #651) are still in their rebase phase — guard is a no-op.

## Cycle output summary

```
PRs classified           : 9 (already labeled from prior cycle, no relabeling this cycle)
Beads filed (new)        : 0
Beads filed (skipped)    : 6 (already exist via UNIQUE on external_ref)
Path:deep dispatches     : 0 new (caro-jac.9 already claim_policy:manual_only)
Refuse-list pre-checks   : 2 deferred (soft-classified pending PR #1087 merge)
Release-readiness gate   : NOT MET (2 unreleased, 18% milestone)
DB desync detected       : YES — bd list ≠ JSONL (3 vs 9 rebase beads)
Hermes digest dropped at : .hermes/messages/pr-dispatch-2026-05-15-0211.md
```

## Hand-off

- **Hermes**: please surface the DB desync in tomorrow's digest under "infrastructure health" and the dispatcher-reframe PR status under "in-flight workstreams".
- **No PR comments posted this cycle**: prior cycle's bead descriptions already frame the forward path for each PR; preempting with new `[agent]` comments would be churn.
- **Next dispatcher run** (cron, ~06:11 UTC): if DB still desynced, this finding repeats — Hermes can dedupe.
