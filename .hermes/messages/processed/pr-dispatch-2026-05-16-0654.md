# Dispatcher cycle — 2026-05-16 06:54 UTC (caro-merge-review-integrate)

**Cycle ID**: head f0d14ce6 · 2026-05-16T06:54Z
**Routine**: `~/.claude/scheduled-tasks/caro-merge-review-integrate/SKILL.md`
**Working tree**: `feat/openrouter-backend` (dispatcher runs are read-only against milestone PRs; no commit needed)

## Forward-path classifications (this cycle)

All 9 open v1.2.0 PRs already carry `path:*` classifications and rebase beads from the 2026-05-13 / 2026-05-15 cycles. Re-classification check confirmed labels intact; no relabeling required.

| PR | path | mergeable | rebase bead | notes |
|---|---|---|---|---|
| #680 | path:scoped | CONFLICTING | caro-jac.3 (open) | i18n automation scripts |
| #652 | path:scoped | CONFLICTING | caro-jac.4 (open) | DCO sign-off requirements |
| #651 | path:auto | UNKNOWN | caro-jac.1 (open) | reusable prompt — docs only |
| #609 | path:auto | UNKNOWN | caro-jac.2 (open) | install.sh CLI flags |
| #605 | path:scoped | CONFLICTING | caro-jac.8 (open) | **soft-classified**: `Cargo.toml` adds `libc = "0.2"` (would violate `deps_only`); `src/main.rs` touch (would violate `no_new_flags`). Strict-mode reclassification deferred until policy.yaml lands on main. |
| #599 | path:deep | MERGEABLE | caro-jac.9 (in_progress, claim_policy:manual_only) | Astro DB + Turso waitlist — claimed by caro-waitlist-engineer |
| #597 | path:scoped | CONFLICTING | caro-jac.7 (open) | slash commands + reference docs |
| #574 | path:scoped | CONFLICTING | caro-jac.6 (open) | **soft-classified**: `install.ps1` is Tier 3 distribution surface (auto_rebase:true, auto_dispatch:false). Bead notes rebase-only constraint. |
| #128 | path:scoped | CONFLICTING | caro-jac.5 (open) | deps skill + skills guide |

**Verdict counts**: 2× path:auto, 6× path:scoped, 1× path:deep, 0× path:refuse-list, 0× path:decision.

## Newly filed beads

**None** — idempotency keys (`gh-${PR}-rebase`) blocked re-filing. All 9 dispatch beads still in `.beads/issues.jsonl` from prior cycles.

## Coordination alerts

### 🔴 P1 (continued) — Local SQLite still out of sync with `.beads/issues.jsonl`

- Session-start git status still shows `.beads/.local_version`, `.beads/.sync.lock`, `.beads/beads.db-shm`, `.beads/beads.db-wal`, `.beads/last-touched`, `.beads/sync_base.jsonl` as untracked — interrupted-sync signature persists across cycles
- `bd list` returns 52 issues; JSONL contains 11 `gh-*-rebase` entries (9 open + 2 closed). The numbers no longer disagree as dramatically as the prior cycle reported, but the sync lock + WAL files indicate an in-flight or aborted sync
- **Suggested fix-up (out of scope for dispatcher)**: on a clean checkout, run `bd config set sync.branch <branch>`, then let `bd` rebuild the SQLite from JSONL and clear the lock files

### 🟡 P2 — Policy ratification: #1087 closed; #1094 is the successor

- **State change from prior cycle**: [PR #1087](https://github.com/wildcard/caro/pull/1087) was **closed at 2026-05-15T03:49Z** by @wildcard with comment "Superseded by #1094 (rebased, CI fixed)"
- [PR #1094](https://github.com/wildcard/caro/pull/1094) — branch `fix/dispatcher-reframe-rebased`, state **OPEN**, mergeable **MERGEABLE** — carries the policy.yaml ratification forward
- ⚠️ #1094 is **not in the v1.2.0 milestone** (milestone: null). It's dispatcher infrastructure, not a v1.2.0 feature. The dispatcher's strict-mode pathway remains blocked until #1094 merges
- **Suggested action**: merge #1094. It is currently the lowest-friction unblock for this routine

### 🟢 No new PRs since 2026-05-15 02:11 UTC cycle

Same 9 PRs (#680, #652, #651, #609, #605, #599, #597, #574, #128). Idle-state on the milestone-PR front.

## Refuse-list deferrals (soft → strict policy gap)

Unchanged from prior cycle. Two PRs would change classification under strict policy reading:

| PR | Current label | Strict-policy label | Policy trigger |
|---|---|---|---|
| #605 | path:scoped | path:refuse-list | `Cargo.toml` adds `libc = "0.2"` — violates `deps_only` constraint |
| #574 | path:scoped | path:scoped (rebase-only) | `install.ps1` Tier 3 distribution; coder-loop must not edit |

Once #1094 lands, run a one-shot dispatcher cycle in strict mode to upgrade labels and post `[agent]` decision-frame comments on #605.

## Release-readiness gates

- **CHANGELOG.md `[Unreleased]` entries**: 2  (threshold: ≥3) — **unchanged**
- **Milestone v1.2.0 progress**: 7 closed / 37 total = **18%** (threshold: ≥80%) — **unchanged**
- **Threshold met**: **NO**

No `/caro.release.prepare` suggestion this cycle.

## Phase 4 authorship guard

No auto-merge attempted. The two `path:auto` PRs (#609, #651) remain `UNKNOWN` mergeability — they have not been rebased by the coder-loop yet. Guard is a no-op.

## Phase 5 — Hermes digest input

This file is the Hermes consumption artifact for this cycle. Hermes is invited to roll this signal into its next daily digest, especially the #1094 unblock recommendation and the persistent beads SQLite-sync issue.

## Cycle output summary

```
PRs classified           : 9 (all carry prior path:* labels; no relabeling)
Beads filed              : 0 new (idempotency keys held; 0 skipped errors)
Path:deep dispatched to  : caro-waitlist-engineer (#599 / caro-jac.9, already in_progress)
Release readiness        : no (2 unreleased, 18% milestone)
Hermes digest dropped at : .hermes/messages/pr-dispatch-2026-05-16-0654.md
Notable state change     : #1087 CLOSED → #1094 OPEN+MERGEABLE (policy ratification successor)
```

## Notes on this cycle's autonomous choices

- **`bd sync` not run** because the working tree is on a feature branch (`feat/openrouter-backend`), no new beads were filed, and the prior cycle flagged the sync-lock files as still present. Forcing a sync from a dirty tree on a non-main branch risks compounding the P1 sync issue. Defer to the next clean-tree session
- **No `[agent]` PR comments posted** because no classification changed
- **No auto-merge attempted** — guard is intact; the path:auto PRs are still pre-rebase
