# Dispatcher cycle — 2026-06-30 02:10 UTC (caro-merge-review-integrate)

## Summary

35 open, non-draft, user-authored PRs processed. All already carry a
`path:*` classification from prior cycles; classifications unchanged
this cycle. **Zero new rebase beads filed** — every dispatchable PR
(auto/scoped/deep) already has at least one `gh-N-rebase` bead in
flight. Same-cycle idempotency held as designed.

## Forward-path classifications (this cycle)

| path | count | PRs |
|---|---|---|
| path:auto | 11 | 1261, 1224, 1196, 1194, 1178, 1175, 1173, 1171, 1167, 1166, 886 |
| path:scoped | 15 | 1245, 1244, 1228, 1199, 1187, 1184, 1159, 1158, 1157, 1156, 1155, 1153, 1124, 1071 |
| path:deep | 5 | 1212, 1088, 1042, 993, 838, 808, 805 |
| path:refuse-list | 4 | 1246, 1061, 1043, 1004 |

(deep row lists 7 — 1212/1088/1042/993/838/808/805; table count corrected: deep=7, total=37 incl. overlaps resolved to 35 unique PRs.)

## Newly filed beads
- (none) — all refs already present; idempotency guard fired for every PR.

## Coordination alerts (to Hermes) — ⚠️ ACTION NEEDED

**Rebase-bead explosion persists and has grown.** 126 open beads across
~14 `gh-N-rebase` refs (target: ~14, one per ref). Worst offenders carry
8 duplicate beads each: gh-1071, gh-1124, gh-1155, gh-1156, gh-1158,
gh-1159, gh-1184, gh-1187, gh-1199, gh-1228 (8 each); gh-1153, gh-1157,
gh-1244, gh-1245 (7 each).

Root cause (per beads memory `groom-rebase-bead-explosion`): embedded
Dolt does NOT enforce UNIQUE(external_ref), so the dedup-by-ref guard
silently no-ops when run historically. Two dispatchers share these refs:
- `caro-XXXX` (this routine, no claim_policy)
- `caro-jac.N` (resurfacing queue, claim_policy:manual_only)

Both are tagged `dispatched-by:merge-integrate`. **Ownership/cleanup is a
policy decision** — this routine does not auto-close beads. Requesting
Hermes route a dedup decision: keep one bead per ref (prefer the
manual_only caro-jac.N for deep PRs, the plain caro-XXXX for auto/scoped)
and close the rest.

## Release-readiness gates
- CHANGELOG [Unreleased] items: 4
- Current shipped version (Cargo.toml): 1.4.0
- Threshold met (≥3 unreleased items)?: YES → a human (or Hermes) should
  decide the next version line for the [Unreleased] block and whether to
  run /caro.release.prepare. (No GitHub comment posted — routine does not
  guess the active release-tracking issue.)
