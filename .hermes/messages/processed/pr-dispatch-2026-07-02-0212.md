# Dispatcher cycle — 2026-07-01 (caro-merge-review-integrate)

## 🚨 Headline: entire PR backlog gated on systemic red CI on `main`

37 open user-authored PRs. **Only 1 is fully green (#1194).** Nearly every
other PR inherits the *same four pre-existing failures* from `main`:

- `Unit Tests (macos-latest)`
- `Unit Tests (ubuntu-latest)`
- `Lint & Format`
- `Knowledge Integration Tests (ubuntu-latest)`

**Keystone: PR #1246** ("fix(ci): repair pre-existing main test failures")
is MERGEABLE and already fixes Unit Tests + Lint — it now fails only
`Knowledge Integration Tests` + `check-labels`. But #1246 is
`path:refuse-list` (touches CI/safety surface, `@wildcard` human-gated).

**Implication:** rebasing PRs onto a red `main` cannot turn them green.
Per the "drain rather than flood" guardrail, this cycle **did NOT** mass-file
rebase beads — the queue can't drain until #1246 (or an equivalent CI fix)
lands. Dispatch was limited to the 2 genuinely-actionable green/near-green PRs.

## Forward-path classifications (this cycle)

All 37 PRs retain prior-cycle `path:*` labels (classification stable — no
git-state change since last cycle). Summary counts:

| path | count | notes |
|---|---|---|
| path:auto | 12 | mostly QA rotations + agent/rule additions |
| path:scoped | 14 | brand/design, i18n, eval, integrations |
| path:deep | 6 | safety allowlist, security probe, eval backends, v1.2 WPs |
| path:refuse-list | 4 | #1246, #1043, #1061, #1004 — CI/workflow/safety surface |
| (unlabeled) | 1 | #1280 demo refresh — inherit-only, non-code |

Mergeable vs conflicting: 24 MERGEABLE, 13 CONFLICTING.

## Newly filed beads (dispatch cap: 15/cycle — used 2, NOT hit)

- `caro-nfdl` — gh-1194-rebase — path:auto — PR #1194 (fully green, docs-only, **ready for human merge-review NOW**)
- `caro-8cuq` — gh-1196-rebase — path:auto — PR #1196 (near-green, 2 inherited failures)

Both parented under epic `caro-jac` (v1.2.0 PR Rebase Queue).

## Coordination alerts (to Hermes)

- **P1 systemic**: main CI is red for a pre-existing reason. Backlog throughput
  is ~0 until #1246 lands. Recommend surfacing #1246 for human review as the
  single highest-leverage merge in the queue.
- Note: bd child-ID generation under epic `caro-jac` throws a UNIQUE-constraint
  error (bd v0.49.3; DB has `.corrupt` backups in-repo). Worked around by
  filing top-level beads + `bd dep add` to the epic. Flag for bd maintenance.

## Release-readiness gate

- CHANGELOG [Unreleased] items: **4** (≥3 threshold **MET**)
- Current shipped version (Cargo.toml): **1.4.0**
- A human (or Hermes) should decide which version line the [Unreleased] block
  belongs to and whether to run `/caro.release.prepare`. **Blocked in practice**
  by the same red-CI state — a release cut from red main is inadvisable.
