# Dispatcher cycle — caro-merge-review-integrate (2026-07-02, cron auto-run)

## Forward-path classifications (this cycle)

36 open, non-draft, user-authored PRs (bots excluded). One newly filed PR
(#1280) classified this cycle; the rest carried prior `path:*` labels.

| PR | path | owner | bead | notes |
|----|------|-------|------|-------|
| 1280 | auto (new) | wildcard | caro-jac.11 | demo/website assets only — no refuse-list glob |
| 1261 | auto | wildcard | caro-jac.12 | mergeable |
| 1224/1178/1175/1171/1167 | auto | wildcard | caro-jac.13–.18 | QA rotation docs |
| 1173 | auto | wildcard | caro-jac.16 | weekly planning report |
| 1245/1244/1228/1199/1187 | scoped | wildcard | caro-jac.19–.23 | mergeable |
| 1159/1158 | scoped | wildcard | caro-jac.24–.25 | brand |
| 1212/1088/1042/993/838/808/805 | deep | wildcard | (prior cycles) | UNIQUE-collision on refile = already dispatched; idempotent |
| 1196/1194 | auto | wildcard | (in-flight) | open bead already existed |
| 1246/1061/1043/1004 | refuse-list | wildcard | none | policy glob fired (workflows / ci / windows-exec) — no bead by design |

## Newly filed beads (15 — cap hit)
caro-jac.11–.25 (parent epic caro-jac / "caro-rebase" queue).
Split: 8 path:auto (P2), 7 path:scoped (P3).

## Deferred (dispatch cap ~15/cycle hit — no silent truncation)
Scoped/auto not filed this cycle, next-cycle candidates:
#1156, #1155, #1124, #1166, #886, #1184, #1157, #1153, #1071.

## Coordination alerts (to Hermes)
- **INFRA**: `.beads` DB/JSONL diverged — `bd doctor` reports 3 sync-divergence
  issues (DB import newer than JSONL; 15 uncommitted `.beads` files; recent
  `.corrupt` snapshots dated 2026-07-01). `bd create` writes to DB but
  `bd list --json` reads stale JSONL, so list-based idempotency under-reports
  in-flight beads. UNIQUE-constraint on refile is the reliable idempotency
  guard. Recommend a `bd sync` / reconcile pass by a maintainer before the
  next cycle. Not auto-remediated (destructive-git caution).

## Release-readiness gate
- CHANGELOG [Unreleased] items: 4
- Current shipped version (Cargo.toml): 1.4.0
- Threshold (>=3 unreleased) met?: YES
- Action: human/Hermes to decide the next version line for the 4 accumulated
  [Unreleased] items and whether to run /caro.release.prepare. Dispatcher does
  not guess a release-tracking issue.
