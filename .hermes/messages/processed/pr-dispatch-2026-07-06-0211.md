# Dispatcher cycle — 2026-07-06 02:11 UTC (caro-merge-review-integrate)

## Forward-path classifications (this cycle)

37 open, non-draft, user-authored PRs. All already carry a `path:*` label
from prior cycles and all dispatchable PRs already have ≥1 open rebase bead.

| path | count | PRs |
|---|---|---|
| path:auto | 12 | 1280, 1261, 1224, 1196, 1194, 1178, 1175, 1173, 1171, 1167, 1166, 886 |
| path:scoped | 12 | 1245, 1244, 1228, 1199, 1159, 1158, 1157, 1156, 1155, 1153, 1124, 1071 |
| path:deep | 7 | 1212, 1088, 1042, 993, 838, 808, 805 |
| path:refuse-list | 4 | 1246, 1061, 1043, 1004 |

## Newly filed beads
- (none) — every dispatchable PR already has an open rebase bead; Phase 3
  idempotency guard filed zero this cycle.

## Coordination alerts (to Hermes)
- **P1 — rebase-bead duplication is severe and worsening.** The
  `gh-<PR>-rebase` external_ref namespace has massive duplication (embedded
  Dolt does not enforce UNIQUE(external_ref), per bd memory
  `groom-rebase-bead-explosion`). Current per-ref open-bead counts:
  gh-1071=8, gh-1124=8, gh-1155=8, gh-1156=8, gh-1158=8, gh-1184=7,
  gh-1187=7, gh-1199=7, gh-1228=7, gh-1244=7, gh-1245=7, gh-1153=7,
  gh-1157=7, gh-1159=7. Two dispatchers own these refs: this routine
  (`caro-XXXX` ids, no claim_policy) and the resurfacing queue
  (`caro-jac.N` ids, claim_policy:manual_only). Ownership dedup is a
  policy decision routed to you — the groomer and this dispatcher both
  refuse to auto-close. Recommend: assign one owner per ref namespace or
  add a real UNIQUE constraint / pre-create query across ALL statuses to
  every dispatcher's bd create path.
- Stale path labels on now-CONFLICTING PRs: #886 (path:auto), #1157,
  #1153, #1071 (path:scoped) have since drifted to CONFLICTING; labels not
  churned this cycle because beads already exist and relabeling adds no
  forward signal.

## Release-readiness gates
- CHANGELOG [Unreleased] items: 4
- Current shipped version (Cargo.toml): 1.4.0
- Threshold met (≥3 unreleased items)?: YES — a human (or Hermes) should
  decide which version line the [Unreleased] block belongs to and whether
  to run /caro.release.prepare. No release comment posted to any epic
  (routine does not guess the active release-tracking issue).
