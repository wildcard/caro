# Dispatcher cycle — 2026-07-12 02:14 UTC (caro-merge-review-integrate)
# ⚠️ CORRECTED after rp-recovery: bead filing FAILED to persist — see below.

## What this cycle actually accomplished
- Classified all 38 open, non-draft, user-authored PRs (forward paths below).
- Confirmed 4 refuse-list PRs unchanged (idempotent): #1246, #1043, #1061, #1004.
- Bead filing DID NOT PERSIST to the authoritative embedded-Dolt store.

## Forward-path classifications (durable output of this cycle)

15 labeled-but-stalled PRs were given a scoped forward path:

| PR | path | owner | note |
|---|---|---|---|
| 1296 | scoped | nightcityblade | src/main.rs rebase-only (no_new_flags) |
| 1245 | scoped | wildcard | agent/** + backends/** rebase-only |
| 1244 | scoped | wildcard | backends/hybrid/sanitizer.rs rebase-only |
| 1184 | scoped | wildcard | backends/remote rebase-only (#1115) |
| 1071 | scoped | wildcard | static_matcher rebase-only; CONFLICTING |
| 1159 | scoped | wildcard | brand — visual audit before merge |
| 1158 | scoped | wildcard | brand — visual audit before merge |
| 1156 | scoped | wildcard | brand — visual audit before merge |
| 1155 | scoped | wildcard | brand + tokens.css — visual audit |
| 1228 | scoped | wildcard | .github/scripts i18n (not workflows/**) |
| 1199 | scoped | wildcard | remotion demo refresh |
| 1187 | scoped | wildcard | additive verify-artifact gate |
| 1124 | scoped | wildcard | .beads/** DB recovery |
| 1157 | scoped | wildcard | docs/rules; CONFLICTING |
| 1153 | scoped | wildcard | integrator branch; CONFLICTING |

## 🔴 CRITICAL: this scheduled task is broken in the cron environment

Root cause (matches bd memory `groom-rebase-bead-explosion`):
1. The dispatcher's idempotency guard queries `bd list --json`, which returns
   a STALE sqlite export (`.beads/beads.db`) AND silently defaults to limit 50
   (memory `groom-bd-list-default-limit-50`). It therefore ALWAYS sees zero
   existing beads and re-files every ref every run.
2. Embedded Dolt does NOT enforce UNIQUE(external_ref) — the dup guard no-ops.
3. Result: 14 rebase refs now carry 7–8 DUPLICATE open beads each (~143 total),
   accumulated one set/day from daily cron runs 2026-06-28 → 2026-07-05.
4. THIS run (2026-07-11): creates hit the corrupt sqlite export and did not
   even reach Dolt — store shows only 1 bead created since 2026-07-06. Split
   brain confirmed: `bd show caro-jac.62` returned gh-1296 (sqlite) early in
   the session, gh-1245 (Dolt) after the prime hook re-resolved the workspace.

## Actions taken / deliberately NOT taken
- Did NOT create beads via re-run — persistence is unreliable and would deepen
  the explosion.
- Did NOT mass-close the ~143 duplicates — per `groom-rebase-bead-explosion`,
  dedup ownership is a Hermes/human policy decision, not an autonomous close.
- Did NOT run `bd init --force` / destructive DB recovery autonomously.

## Requests to Hermes / human (interactive session needed)
1. Dedup the rebase-ref explosion (~143 beads → 1 canonical per ref). Owner
   policy: caro-jac.* (resurfacing, manual_only) vs caro-XXXX (merge-integrate).
2. Repair `.beads/beads.db` sqlite export corruption (regenerate from Dolt) OR
   move the cron to server mode so it talks to Dolt directly.
3. FIX THE DISPATCHER before the next cron run: the idempotency guard MUST
   query existing external_ref across ALL statuses via `bd` against the
   authoritative store with `--limit` > total-open — never via raw sqlite or
   default-limit `bd list`. Until fixed, this task re-explodes daily.

## Release-readiness gate
- CHANGELOG [Unreleased] items: 4 ; shipped version: 1.4.0 ; threshold (>=3): MET
- Recommendation: human/Hermes decides the next version line + whether to run
  /caro.release.prepare. Dispatcher does not guess the release-tracking issue.

## Raw backup (non-destructive)
- `.beads/backup/preserve-newbeads-20260712-0215/` holds the corrupt db+wal+shm
  as-is. Given creates never reached Dolt, it is a forensic artifact only.
