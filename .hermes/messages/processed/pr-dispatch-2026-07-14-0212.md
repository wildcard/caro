# Dispatcher cycle — 2026-07-14 02:12 UTC (caro-merge-review-integrate)

## Scope

42 open, non-draft, user-authored PRs. 35 already carry a `gh-<N>-rebase`
bead (caro-jac.* queue) from prior cycles — idempotency-skipped. 7 were
untracked and classified this cycle.

## Forward-path classifications (this cycle)

| PR | path | owner | bead filed | notes |
|---|---|---|---|---|
| #1323 | path:auto | wildcard | none (merge-ready) | MERGEABLE + green CI. Forward path = human merge, not rebase. Merge candidate. |
| #1322 | path:scoped | wildcard | none | MERGEABLE but Vercel deploy + `validate` CI red. Needs i18n/validate fix (he/landing.json), not a rebase. |
| #1314 | path:refuse-list | wildcard | none | Touches `.github/workflows/**` (automation-scheduler.yml). CONFLICTING. Decision comment posted. |
| #1308 | path:refuse-list | wildcard | none | Touches `.github/workflows/packages.yml`. CONFLICTING. Decision comment posted. |
| #1061 | path:refuse-list | wildcard | none (pre-labeled) | workflows/ci.yml. Already refuse-list from prior cycle. |
| #1043 | path:refuse-list | wildcard | none (pre-labeled) | workflows (ci.yml + release.yml); CI red; needs-human. |
| #1004 | path:refuse-list | wildcard | none (pre-labeled) | workflows/validate-translations.yml; needs-human. |

## Newly filed beads
- (none) — the 2 mergeable untracked PRs need merge/CI-fix not rebase; the
  4 conflicting untracked PRs are all refuse-list (workflows glob), which
  per routine do not get beads. Rebase queue is well-drained.

## Coordination alerts (to Hermes)
- #1323 is MERGEABLE with green CI and only touches `website/**`
  (LPMoments contrast fix + test). Strong merge candidate — surface to a
  human for review/merge.
- #1322 is a 1-line i18n fix blocked only by a red `validate` + Vercel
  deploy. Small unblock; worth a human glance.

## Release-readiness gates
- CHANGELOG [Unreleased] items: 4
- Current shipped version (Cargo.toml): 1.4.0
- Threshold met (>=3 unreleased items)?: YES
- Action: a human (or Hermes) should decide which version line the
  [Unreleased] block belongs to and whether to run /caro.release.prepare.
  Dispatcher does not guess a release-tracking issue.
