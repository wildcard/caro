# Dispatcher cycle — 2026-07-19 02:10 UTC (caro-merge-review-integrate)

## Forward-path classifications (this cycle)

45 open non-draft user-authored PRs. 42 already carried a `path:*` label from
prior cycles and an in-flight bead (or a refuse-list decision); 3 were new.

| PR | path | owner | bead filed | notes |
|---|---|---|---|---|
| 1347 | refuse-list | @wildcard | no (by design) | `.github/workflows/translate.yml` → Tier 1 glob `.github/workflows/**`. MERGEABLE, green. Decision comment posted. |
| 1344 | refuse-list | @wildcard | no (by design) | `.github/workflows/slash-router.yml` → same Tier 1 glob. MERGEABLE, green. Decision comment posted. |
| 1338 | auto | coder-loop | caro-jac.86 | `src/main.rs` Tier 2 `no_new_flags` constraint **verified satisfied** (zero added clap flags). MERGEABLE, green. |

### Standing distribution (all 45)

- path:auto — 17
- path:scoped — 12
- path:deep — 8 (`claim_policy:manual_only`; delivery-agent work)
- path:refuse-list — 8 (5 pre-existing + 1347, 1344 new; 1314/1308/1061/1043/1004 unchanged)

## Newly filed beads

- `caro-jac.86` — `gh-1338-rebase` — `pr-dispatch,loop:rebase,path:auto,dispatched-by:merge-integrate`

Dispatch cap (~15/cycle) not approached. No silent truncation.

## Coordination alerts (to Hermes)

- **Stale `.git/index.lock` in the main worktree** (zero-byte, created 2026-07-17 02:36 UTC, ~40h old; no `git` process alive). It did not block bead creation in embedded Dolt, but it **blocks bd's JSONL export auto-commit**, so `.beads/issues.jsonl` on disk is behind the DB. The dispatcher does not remove lock files autonomously — flagging for a human or the next interactive session to clear (`rm .git/index.lock` after confirming no live git process).
- 17 `path:auto` PRs are accumulating with green CI. The rebase queue is being filed faster than the coder-loop drains it; worth a Hermes note on coder-loop throughput.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: 4
- Current shipped version (Cargo.toml): 1.4.0
- Threshold met (≥3 unreleased items)?: **YES**

Four `[Unreleased]` items have accumulated against shipped v1.4.0. A human (or
Hermes) should decide which version line the block belongs to and whether to run
`/caro.release.prepare`. The dispatcher does not guess the active
release-tracking issue and posts no GitHub comment for this.
