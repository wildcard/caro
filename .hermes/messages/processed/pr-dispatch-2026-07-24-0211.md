# Dispatcher cycle — 2026-07-23 (caro-merge-review-integrate)

## Summary

Steady state on dispatch; the cycle's real work was **queue reconciliation**.
Zero new beads filed — all 51 open user-authored PRs are already either tracked
(44) or correctly refuse-listed (7). 11 tombstone beads were closed.

## Forward-path classifications (this cycle)

| PR | path | mergeable | bead filed | notes |
|---|---|---|---|---|
| 1347 | refuse-list | yes | no (correct) | `.github/workflows/translate.yml` |
| 1344 | refuse-list | yes | no (correct) | `.github/workflows/slash-router.yml` |
| 1314 | refuse-list | yes | no (correct) | workflows + `src/agent/**` + `src/main.rs` |
| 1308 | refuse-list | no | no (correct) | `.github/workflows/packages.yml` |
| 1061 | refuse-list | no | no (correct) | `.github/workflows/ci.yml` |
| 1043 | refuse-list | yes | no (correct) | workflows + `src/backends/**` + `src/prompts/**` |
| 1004 | refuse-list | no | no (correct) | workflows + `src/backends/static_matcher.rs` |
| (44 others) | auto/scoped/deep | mixed | pre-existing | already in caro-jac queue |

All 7 refuse-list verdicts were **re-verified against current file lists**, not
inherited from the stale label. Every one hits `.github/workflows/**`
(auto_dispatch=false, auto_rebase=false) — the strictest refuse glob. All 7
already carry an `[agent]` decision comment; no comments re-posted.

## Newly filed beads

None. Dispatch idempotency held — same git state, zero new beads (the
contract's stated success condition).

## Queue reconciliation (new this cycle)

11 rebase beads referenced PRs that are no longer open. Closed with evidence:

| bead | ref | PR state |
|---|---|---|
| caro-jac.1 | gh-651-rebase | CLOSED |
| caro-jac.2 | gh-609-rebase | CLOSED |
| caro-jac.3 | gh-680-rebase | CLOSED |
| caro-jac.4 | gh-652-rebase | CLOSED |
| caro-jac.5 | gh-128-rebase | CLOSED |
| caro-jac.6 | gh-574-rebase | CLOSED |
| caro-jac.7 | gh-597-rebase | CLOSED |
| caro-jac.8 | gh-605-rebase | CLOSED |
| caro-jac.9 | gh-599-rebase | MERGED |
| caro-jac.16 | gh-1173-rebase | CLOSED |
| caro-jac.20 | gh-1244-rebase | MERGED |

Queue: 55 → 44 open, now exactly matching the 44 dispatch-eligible open PRs.

## Coordination alerts (to Hermes)

**The rebase queue is not draining.** Before this cycle: 54 open / 1
in_progress. The single in_progress bead (caro-jac.9) was itself a tombstone
for a PR merged long ago — meaning the effective in-flight count was **zero**.
The coder-loop has not consumed from caro-jac in some time.

Root cause of the tombstone accretion: the dispatcher's idempotency guard keys
on `external_ref` across *all* statuses (deliberately — it is the fix for the
2026-06/07 99-bead explosion). That guard is one-directional: it prevents
re-filing but nothing propagates PR closure back to the bead. The 11 tombstones
were the **oldest** beads in the queue, so any age-ordered ready list served
them first. Worth a Hermes flag as to whether that starved the coder-loop or
merely masked an unrelated stall.

Suggested follow-up (not actioned — outside this routine's write boundary):
make the reconciliation pass a standing Phase 0 of this routine rather than an
ad-hoc sweep.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4**
- Current shipped version (Cargo.toml): **1.4.0**
- Threshold met (>= 3 unreleased items): **YES**

A human or Hermes should decide which version line the `[Unreleased]` block
belongs to and whether to run `/caro.release.prepare`. No epic comment posted
(the routine does not guess the active release-tracking issue).
