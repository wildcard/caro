# Dispatcher cycle — 2026-07-26 02:09 UTC (caro-merge-review-integrate)

## Cycle character: reconciliation, not dispatch

51 open non-draft user-authored PRs. **All 51 already carried a `path:*`
label** from prior cycles, and all 44 dispatchable PRs already have a
rebase bead. Dedup index built with `bd list --limit 3000` (guards the
silent-50 truncation that caused the 2026-06/07 bead explosion).

## Forward-path distribution (this cycle)

| path | count | notes |
|---|---|---|
| path:auto | 21 | mostly QA-rotation + docs PRs, all MERGEABLE |
| path:scoped | 15 | includes PR #886 reclassified this cycle |
| path:deep | 8 | #805 #808 #838 #993 #1042 #1088 #1212 #1303 #1311 — all CONFLICTING |
| path:refuse-list | 7 | #1004 #1043 #1061 #1308 #1314 #1344 #1347 — policy-gated, no beads by design |

## Changes made this cycle

- **PR #886** — reclassified `path:auto` → `path:scoped`. It is labelled
  auto but has been CONFLICTING for ~5 weeks. It touches only
  `.claude/memory/qa-*.md`, but those are append-only rotation logs that
  have accumulated ~9 months of later QA rotations on main; resolving is
  targeted hand-editing, not a mechanical replay. Bead `caro-e73o`
  re-labelled and re-prioritised P2 → P3 to match.

## Newly filed beads

None. All 44 dispatchable PRs already tracked (45 rebase beads exist —
one covers a PR that has since closed). Dispatch cap (~15/cycle) not
approached.

## Coordination alerts (to Hermes)

- **Drain rate is the concern, not intake.** The rebase queue under
  `caro-jac` is saturated at 45 beads and grew by 0 this cycle. The
  bottleneck is consumption by `/caro-coder-loop`, not classification.
  The 8 `path:deep` beads carry `claim_policy:manual_only` and will
  never drain without a named delivery agent being pointed at them.
- **Five PRs are >6 months old and CONFLICTING** (#805, #808, #838,
  #886, #993). All are `path:deep`/`path:scoped`. Worth a human decision
  on whether the *idea* still stands even though the branch does not —
  the forward path may be "re-cut from current main" rather than
  "rebase".

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4**
- Current shipped version (Cargo.toml): **1.4.0**
- Threshold met (>=3 unreleased items): **YES**

A human or Hermes should decide which version line the `[Unreleased]`
block belongs to (1.4.1 vs 1.5.0) and whether to run
`/caro.release.prepare`. This routine deliberately does not guess the
active release-tracking issue.
