# Dispatcher cycle — 2026-07-27 02:10 UTC (caro-merge-review-integrate)

## Forward-path classifications (this cycle)

54 open non-draft user-authored PRs scanned. 52 already carried a `path:*`
label from prior cycles (no reclassification needed — state unchanged).
Two were newly classified:

| PR | path | owner | bead filed | notes |
|---|---|---|---|---|
| #1374 | path:scoped | wildcard | caro-jac (gh-1374-rebase) | 160 changed files, only 2 substantive (`website/src/layouts/Layout.astro`, `website/src/pages/[lang]/index.astro`); the other 158 are committed runtime state (`.beads/**`, `.hermes/messages/**`, `.claude/memory/**`) and are the entire conflict surface |
| #1373 | path:auto | wildcard | caro-jac.95 (gh-1373-rebase) | 4 QA memory docs, MERGEABLE today |

Standing distribution across all 54: auto 22 · scoped 16 · deep 8 ·
refuse-list 6 · newly-labeled 2 (counted above).

## Newly filed beads
- `caro-jac` child — `gh-1374-rebase` — pr-dispatch, loop:rebase, path:scoped, dispatched-by:merge-integrate (P3)
- `caro-jac.95` — `gh-1373-rebase` — pr-dispatch, loop:rebase, path:auto, dispatched-by:merge-integrate (P2)

Dispatch cap (~15/cycle) not reached — only 2 PRs were undispatched.

## Coordination alerts (to Hermes)

- **QA-rotation PR pile-up.** Six scheduled QA rotation PRs are open
  simultaneously and MERGEABLE: #1373, #1371, #1367, #1363, #1333, #1321.
  All six append to the same four files under `.claude/memory/qa-*.md`.
  They are mergeable only because none has merged yet — the first merge
  will convert the other five to CONFLICTING. Recommend draining them in
  ascending PR order in one sitting rather than letting the daily cron add
  a seventh.
- **Committed runtime state is manufacturing conflicts.** #1374 is the
  clearest case: a 2-file website change is CONFLICTING purely because the
  branch also carries 158 files of `.beads/` / `.hermes/` / `.claude/memory/`
  state. This pattern will keep recurring until those paths are either
  gitignored or excluded from feature branches. Worth a policy decision.

## Release-readiness gates
- CHANGELOG [Unreleased] items: 4
- Current shipped version (Cargo.toml): 1.4.0
- Threshold met (>=3 unreleased items)?: YES — a human (or Hermes) should
  decide which version line the `[Unreleased]` block belongs to and whether
  to run `/caro.release.prepare`. The dispatcher does not pick the version.
