# Dispatcher cycle — 2026-08-10 02:08 UTC (caro-merge-review-integrate)

## Headline

**The rebase queue has no consumer.** 50 beads filed, 0 ever claimed, 0 ever
closed. This cycle filed 0 new rebase beads (queue is at exact 1:1 parity with
open dispatchable PRs) and instead filed one P0 pipeline-defect bead.

## Forward-path classifications (this cycle)

All 57 open, non-draft, user-authored PRs already carry exactly one `path:*`
label from prior cycles. **Zero classification drift detected** — no `path:auto`
PR has become CONFLICTING, and no `path:deep` PR has become MERGEABLE.

| path | PRs | beads filed | avg age | oldest |
|---|---|---|---|---|
| path:auto | 25 | 25 (all pre-existing) | 39d | #1166 @ 78d |
| path:scoped | 16 | 16 (all pre-existing) | 54d | #886 @ 105d |
| path:deep | 9 | 9 (all pre-existing) | 89d | #805 @ 136d |
| path:refuse-list | 7 | 0 (by contract) | 55d | #1004 @ 103d |

Set-difference check: PRs needing a bead = 50; rebase beads present = 50;
symmetric difference = 0. Dispatch idempotency is holding correctly.

## Newly filed beads

- `caro-jac.100` — `pipeline-rebase-queue-no-consumer` — P0 —
  labels: `pr-dispatch,pipeline-defect,dispatched-by:merge-integrate,claim_policy:manual_only`

## Coordination alerts (to Hermes)

**ALERT 1 — write-only queue (P0, ~4 months standing).**
`.claude/commands/caro-coder-loop.md:33` explicitly excludes `loop:rebase`
beads, deferring them to a "rebase loop (follow-up)" that was never built.
A grep for `loop:rebase` across `.claude/` and `~/.claude/` (excluding stale
worktree copies) returns exactly two live files: the dispatcher SKILL.md that
*writes* the label and the coder-loop that *filters it out*. The dispatcher's
own Phase 3 text claims `/caro-coder-loop` will claim these beads — that is
the opposite of what the coder loop does. Contract and implementation disagree.

**ALERT 2 — the queue is mis-shaped even if a consumer existed.**
25 of the 50 tracked PRs are already `MERGEABLE` with no conflicts, so a
"rebase" is a no-op for half the queue. Their real blocker is review/merge
capacity: no open PR carries an approving review. Building a rebase consumer
would not move these 25. `caro-jac.96` ("Drain the six mergeable QA-rotation
PRs") already noticed this and is itself stuck behind `claim_policy:manual_only`.

**ALERT 3 — label hygiene defect on dispatcher-owned beads.**
47/50 rebase beads carry `claim_policy:manual_only` though only 9 PRs are
`path:deep`; 12 beads carry two `path:*` labels from cross-cycle stacking.
Deliberately NOT repaired this cycle: it is moot while `loop:rebase` alone is
disqualifying, and the correct label set depends on which resolution (A/B/C in
`caro-jac.100`) is chosen. Repair it alongside that decision.

**ALERT 4 — stale git locks blocking `bd sync`.**
`.git/index.lock` (0 bytes, Jul 23) and `.beads/.sync.lock` (0 bytes, Jul 11)
are stale with no running git process. `bd`'s git-sync step fails against them,
so this cycle's bead did not push to origin. Removal not performed
autonomously (deletion requires operator confirmation).

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4**
- Current shipped version (Cargo.toml): **1.4.0**
- Threshold met (≥3 unreleased items)?: **YES**

A human or Hermes should decide which version line the `[Unreleased]` block
belongs to and whether to run `/caro.release.prepare`. No GitHub comment was
posted — the routine does not guess the active release-tracking issue.
