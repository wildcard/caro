# Dispatcher cycle — 2026-08-26 06:45 UTC (caro-merge-review-integrate)

## Forward-path classifications (this cycle)

All 59 open, non-draft, user-authored PRs already carry a `path:*` label from
prior cycles. No reclassification was warranted — no PR changed mergeability
class or acquired a new decision-blocking label since the last run.

| path | count | mergeable | conflicting |
|---|---|---|---|
| `path:auto` | 25 | 25 | 0 |
| `path:scoped` | 17 | 8 | 9 |
| `path:deep` | 9 | 0 | 9 |
| `path:refuse-list` | 8 | 5 | 3 |
| **total** | **59** | 38 | 21 |

Notable: **the entire `path:auto` lane is `MERGEABLE`** — 25/25 conflict-free.
Conversely **every `path:deep` PR is `CONFLICTING`** (9/9), which is the
expected shape: deep PRs are the ones with structural/product conflicts, and
they carry `claim_policy:manual_only` so the coder-loop leaves them alone.

## Newly filed beads

**None.** Full dispatch idempotency this cycle.

- Dispatchable PRs (auto + scoped + deep): 51
- Existing `gh-<N>-rebase` beads covering them: 51
- Missing beads: **0**
- `path:refuse-list` PRs wrongly carrying a bead: **0** (correct — refuse-list
  waits on policy adjustment, not coder work)
- Orphan rebase beads (bead open, PR no longer open): **0**

Bead-store health: 264 total beads, 95 with a `gh-` external_ref. Queried with
`bd list --limit 3000` against the embedded-Dolt store, per the
`groom-bd-list-default-limit-50` memory — the default 50-row truncation is what
caused the historical 99-bead rebase explosion.

## Coordination alerts (to Hermes)

- **Drain backlog is lopsided.** 25 conflict-free `path:auto` beads are sitting
  ready while the coder-loop's throughput is the binding constraint, not
  classification. The dispatcher has nothing left to file; the queue needs
  consumption, not more dispatch. Worth surfacing in the digest as a
  coder-loop capacity signal rather than a PR-health signal.
- **`path:deep` lane is 100% conflicted and 100% manual-only.** These 9 PRs
  (#1311, #1303, #1212, #1088, #1042, #993, #838, #808, #805) cannot progress
  without a named delivery agent or human. Several are long-lived v1.2-era
  feature branches whose conflicts compound each cycle.
- No parallel-branch/duplicate-work conflicts detected.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4** (threshold ≥3 — **met**)
- Current shipped version (`Cargo.toml`): **1.4.0**
- Threshold met?: **YES**

Unreleased items awaiting a version line:
1. Runtime-loadable custom safety patterns via TOML config
2. MSRV bumped 1.83 → 1.85 (CI breakage from transitive deps)
3. Static matcher: "find and kill the runaway process eating CPU" now emits
   `kill -SIG` commands
4. Bump `rustls-webpki` 0.103.10 → 0.103.13 (advisory resolution)

Item 2 (MSRV bump) is a **breaking-ish change for downstream consumers** and
item 1 is a net-new user-facing feature — that shape argues for **v1.5.0**
rather than a patch. A human (or Hermes) should decide the version line and
whether to run `/caro.release.prepare`. The dispatcher does not guess the
active release-tracking issue and posts no GitHub comment.
