# Dispatcher cycle — 2026-09-04 03:21 UTC (caro-merge-review-integrate)

## Cycle verdict: STEADY STATE — 0 new beads filed

60 open, non-draft, user-authored PRs classified. All 60 already carried a
`path:*` label from a prior cycle; classification re-verified against current
`mergeable` state and found consistent (no `path:auto` PR has drifted into
`CONFLICTING`).

## Forward-path distribution (this cycle)

| path | count | dispatchable | beads exist |
|---|---|---|---|
| `path:auto`        | 26 | yes | 26 |
| `path:scoped`      | 17 | yes | 17 |
| `path:deep`        |  9 | yes |  9 |
| `path:refuse-list` |  8 | no  | n/a |
| **total**          | 60 |     | 52 |

Idempotency check passed exactly: the set of dispatchable PRs
(`gh-<N>-rebase`) and the set of existing rebase beads are identical —
zero missing, zero orphaned. `bd list --limit 3000` over 265 total issues
(limit well above the 50-default truncation hazard).

## Newly filed beads

None. Re-running the dispatcher against unchanged git state filed zero
beads, which is the contract.

## Drain signal — highest-value `path:auto` candidates

Ten `path:auto` PRs currently have **zero failing checks**:

`#1389` `#1386` `#1373` `#1371` `#1367` `#1338` `#1333` `#1321` `#1320` `#1194`

None carry an approving review (`reviewDecision` is empty on every open PR),
so the merge gate ("≥1 human-approved review" + Phase 4 authorship guard)
holds. These are the cheapest PRs for a human to land.

## Coordination alerts (to Hermes)

- **CI rot on the long-tail QA-rotation branches.** The oldest `path:auto`
  PRs are accumulating failures purely from age, not from their own diffs:
  `#1166` (9 failing), `#1171` (9), `#1175` (8), `#1178` (8), `#1167` (8),
  `#1299` (8), `#1300` (7), `#1280` (6). These are `chore(qa)` rotation
  reports — content is append-only logs, so the failures are almost
  certainly base-drift, not regressions. Suggest a batched close-or-land
  decision rather than 8 separate rebases; each rebase burns coder-loop
  time on a report that has already been superseded by later rotations.
- No parallel-branch conflicts detected against existing rebase beads.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4** (threshold ≥3 — **met**)
- Current shipped version (Cargo.toml): **1.4.0**
- Headline unreleased content:
  - Added: runtime-loadable custom safety patterns via TOML
    (`[[safety.custom_patterns]]` + `~/.config/caro/patterns.toml`)
  - Changed: **MSRV bumped 1.83 → 1.85** (edition2024 transitive requirement)
- **Human/Hermes decision needed**: the MSRV bump is a compatibility-visible
  change. Whether `[Unreleased]` belongs on a `1.5.0` line (new feature +
  MSRV floor raise) rather than a `1.4.x` patch is a release-line call this
  routine deliberately does not make. No GitHub comment posted — no active
  release-tracking issue is known to this routine, and guessing one is the
  stale-epic hazard the gate was rewritten to avoid.
