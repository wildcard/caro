# Dispatcher cycle — 2026-09-06 05:30 UTC (caro-merge-review-integrate)

## Verdict: converged producer, absent consumer

Zero new beads filed. Zero duplicates. Zero orphans. Zero classification
drift. The dispatcher is in exact steady state — and that is precisely what
makes the missing consumer the sole remaining bottleneck.

## Forward-path classifications (this cycle)

61 open, non-draft, user-authored PRs. All already carry exactly one
`path:*` label from prior cycles; none required relabeling.

| path | PRs | beads | oldest idle | notes |
|---|---|---|---|---|
| `path:auto` | 27 | 27 | 84d | mechanical rebase; P2 |
| `path:scoped` | 17 | 17 | 84d | targeted hand-edits; P3 |
| `path:deep` | 9 | 9 | 84d | `claim_policy:manual_only`; P1 |
| `path:refuse-list` | 8 | 0 | 75d | correctly no bead — policy-gated |
| **total** | **61** | **53** | | |

Refuse-list PRs (no bead by contract): #1004, #1043, #1061, #1308, #1314,
#1344, #1347, #1409.

## Newly filed beads

**None.** Idempotency guard held: all 53 dispatchable PRs already have a
bead keyed on `external_ref = gh-<N>-rebase`. The guard was run with
`bd list --limit 3000` against 268 total beads — the default limit of 50
would have truncated to 19% of the store and re-filed a full duplicate set
(the 2026-06/07 explosion mode).

## Coordination alerts (to Hermes)

### 1. Rebase queue still has no consumer — `caro-jac.100` (P0, open 27d)

- **53** rebase beads (was 50 on 2026-08-10). Status: 53 open, 0 in_progress,
  **0 ever claimed**.
- `.claude/commands/caro-coder-loop.md:33` still excludes them:
  `select((.labels // []) | index("loop:rebase") | not)`. The rebase loop
  that :27-28 defers to was never built.
- Oldest bead `caro-jac.11` (gh-1280-rebase) created 2026-06-14 — 84 days
  unclaimed.
- A/B/C decision pending 27 days. **This is the single highest-value
  unblock in the pipeline.**

Both secondary defects named in that bead are now **RESOLVED** (verified
this cycle): `claim_policy:manual_only` is on exactly 9 beads matching the
9 `path:deep` PRs (was 47 of 50); beads carrying >1 `path:*` label is now
0 (was 12). Whichever consumer is chosen inherits clean labels.

### 2. Repo audit trail frozen 7 weeks — `caro-gkry` (P1, open)

`.git/index.lock` — 0 bytes, mtime 2026-07-23 02:36, no live holder — has
blocked every auto-commit since. HEAD is still `50859b89` (2026-07-16).
Every dispatcher and groomer artifact for ~7 weeks is on disk but
uncommitted, including this digest. No data loss (bd writes land in Dolt;
export-state synced 2026-09-05). Fix is two `rm`s, deliberately deferred
per the destructive-commands rule — needs a human to authorize.

### 3. `bd sync` does not exist — `caro-gr3p` (P3, open)

bd 1.0.4 errors `unknown command "sync"`. This routine's own guardrail
still instructs it. Not run this cycle; the daemon auto-export to
`.beads/issues.jsonl` is the real mechanism.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4** (threshold ≥3 — **met**)
- Current shipped version (Cargo.toml): **1.4.0**
- Threshold met?: **YES**

Unreleased block contains: runtime-loadable custom safety patterns (TOML),
MSRV 1.83→1.85, static-matcher runaway-process fix, `rustls-webpki`
0.103.10→0.103.13.

Per contract this routine does not guess a release-tracking issue. Surfacing
for a human or Hermes to decide the version line and whether to run
`/caro.release.prepare`. Note that with `caro-gkry` unresolved, a release
PR cannot be committed from this worktree.
