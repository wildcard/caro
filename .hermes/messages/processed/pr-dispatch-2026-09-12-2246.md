# Dispatcher cycle — 2026-09-12 22:46 UTC (caro-merge-review-integrate)

## Scope

68 open, non-draft, user-authored PRs (87 total open incl. bots).
57 already carry a `gh-<N>-rebase` bead from prior cycles; 9 are
correctly classified `path:refuse-list` (no bead by design).
**11 PRs had no bead — 9 of those were already-commented refuse-list
PRs, leaving 2 genuinely new PRs to dispatch this cycle.**

## Forward-path classifications (this cycle)

| PR | path | owner | bead filed | notes |
|---|---|---|---|---|
| #1447 | path:auto | wildcard | caro-jac.108 | single new `docs/research/*.md`; additive, no glob match |
| #1448 | path:auto | wildcard | caro-jac.109 | single new `docs/research/*.md`; additive, no glob match |
| #1004 | path:refuse-list | wildcard | — | `.github/workflows/validate-translations.yml` (Tier 1) |
| #1043 | path:refuse-list | kukoshock | — | `.github/workflows/{ci,release}.yml` + `src/main.rs` + `src/prompts/**` |
| #1061 | path:refuse-list | wildcard | — | `.github/workflows/ci.yml` (Tier 1) |
| #1308 | path:refuse-list | wildcard | — | `.github/workflows/packages.yml` (Tier 1) |
| #1314 | path:refuse-list | wildcard | — | `.github/workflows/automation-scheduler.yml` + `src/agent/**` + `src/main.rs` |
| #1344 | path:refuse-list | wildcard | — | `.github/workflows/slash-router.yml` (Tier 1) |
| #1347 | path:refuse-list | wildcard | — | `.github/workflows/translate.yml` (Tier 1) |
| #1409 | path:refuse-list | wildcard | — | `.github/workflows/evaluation.yml` + `Cargo.toml` |
| #1438 | path:refuse-list | wildcard | — | 3× `.github/workflows/**` + `Cargo.toml` (Tier 1) |

All 9 refuse-list PRs were re-verified against their actual `gh pr view
--json files` output, not merely trusted from the existing label. All 9
already carry an `[agent]` decision comment from a prior cycle — no new
comments posted (comment idempotency preserved).

## Newly filed beads

- `caro-jac.108` — `gh-1447-rebase` — pr-dispatch, loop:rebase, path:auto, dispatched-by:merge-integrate (P2)
- `caro-jac.109` — `gh-1448-rebase` — pr-dispatch, loop:rebase, path:auto, dispatched-by:merge-integrate (P2)

## Coordination alerts (to Hermes)

1. **Stale `.git/index.lock` in the main worktree** (dated 2026-07-23,
   zero bytes). `bd create` emitted a git-sync warning against it this
   cycle; bead creation still succeeded, but any agent running git
   write operations in `/Users/kobik-private/workspace/caro` will hit
   it. Not removed autonomously — destructive-command policy requires
   explicit human confirmation. **Recommend a human run
   `rm -f .git/index.lock` after confirming no git process is live.**

2. **Duplicate refuse-list decision comments on #1308 and #1314** (2
   each vs 1 elsewhere). A prior cycle re-posted. The dispatcher
   contract specifies bead-filing idempotency but is silent on
   comment idempotency for the refuse-list branch — worth tightening
   in the SKILL.md so refuse-list comments are posted once per
   (PR, policy-version) rather than once per cycle.

3. **Repo-wide `Security Audit` check failing** on PRs that cannot
   have caused it (#1447 and #1448 are single docs files). Open fix
   PR #1311 (`fix/rustsec-crossbeam-quinn`) is itself CONFLICTING and
   already beaded. Every new PR will inherit a red check until it
   lands — this is the highest-leverage single merge in the queue.

4. **Rebase-queue depth: 59 open child beads under `caro-jac`.** The
   queue is draining slower than PRs arrive. 25 of 68 open PRs are
   CONFLICTING. Consider whether `/caro-coder-loop` cadence is
   sufficient or whether a dedicated rebase-drain pass is warranted.

## Release-readiness gates

- CHANGELOG `[Unreleased]` items: **4**
- Current shipped version (Cargo.toml): **1.4.0**
- Threshold met (≥3 unreleased items)?: **YES**

The `[Unreleased]` block leads with runtime-loadable custom safety
patterns (TOML config, `[[safety.custom_patterns]]`) — a user-facing
Added item, which reads as a minor bump (1.5.0) rather than a patch.
No GitHub comment was posted: this routine does not know the active
release-tracking issue and must not guess one. **A human or Hermes
should decide the version line and whether to run
`/caro.release.prepare`.**
