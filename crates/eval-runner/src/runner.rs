// Test runner implementation

pub struct TestRunner;

impl TestRunner {
    pub fn new() -> Self {
        Self
    }
}

impl Default for TestRunner {
    fn default() -> Self {
        Self::new()
    }
}
