# caro-research--scoping-process — Run Report

**Date**: 2026-06-02  
**Agent**: Automated scheduled task (`caro-research--scoping-process`)  
**Status**: ✅ Completed — freshness pass only (no new scoping required)

---

## What Happened This Run

The scheduled task's SKILL.md contains an unfilled `[FEATURE NAME]` placeholder.
No feature target was passed as an argument. The agent autonomously resolved
this by inspecting project context and determining the most relevant v2.0.0
feature to research: **`caro mcp serve`** (issue #928, ADR-015).

---

## Finding: ADR-015 Already Covers This Feature

`docs/adr/ADR-015-mcp-safety-server.md` was produced by the **previous run**
of this same scheduled agent (2026-05-29). The scoping is complete and
comprehensive. No new Phase 1–3 research was needed.

---

## Finding: rmcp Version Drift (CRITICAL — Corrected)

ADR-015 referenced `rmcp = "0.16"`. As of 2026-06-02, the current release is
**`rmcp = 1.7.0`** — a major version bump with breaking API changes.

**Action taken**: ADR-015 updated in-place:
- All `"0.16"` version pins corrected to `"1"` (semver major, tracks 1.x line)
- Consequences section updated: 0.x → 1.x handler API is not compatible
- Alternative C updated to reflect current rmcp version
- `Last reviewed` field added to ADR header

---

## Finding: No Implementation Has Started

- `rmcp` is not in `Cargo.toml`
- `src/mcp/` directory does not exist
- `src/bin/caro-mcp.rs` does not exist
- The build spike (per `.claude/rules/external-sdk-integration.md`) has **not** been run

**Implication**: v2.0.0 deadline is 2026-06-30 (28 days). The build spike must
start this week to leave time for the feature PR and CI validation.

---

## Recommended Next Actions

| Priority | Action | Owner |
|----------|--------|-------|
| P0 | Fix `[FEATURE NAME]` placeholder in SKILL.md so future runs target correctly | Kobi |
| P1 | Run build spike: add `rmcp = { version = "1", optional = true }` + `cargo check --features mcp-server` | caro-coder-loop |
| P1 | Confirm rmcp 1.x MSRV satisfies Rust ≥ 1.85 during spike | build spike PR |
| P2 | Open feature PR after spike merges green | caro-coder-loop |

---

## Files Changed This Run

| File | Change |
|------|--------|
| `docs/adr/ADR-015-mcp-safety-server.md` | Version corrections: `rmcp 0.16` → `"1"`; added `Last reviewed` header; updated risks and alternatives sections |
| `docs/research/caro-research-scoping-2026-06-02.md` | This report |

---

*Generated by `caro-research--scoping-process` · 2026-06-02*
