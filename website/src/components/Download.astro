---
import { SITE_CONFIG } from '../config/site';
import { t, type Locale } from '../i18n/config';

interface Props {
  lang?: Locale;
}

const { lang = 'en' } = Astro.props;

// Get translated button labels for client-side JS
const copyLabel = t(lang, 'common.buttons.copy');
const copiedLabel = t(lang, 'common.buttons.copied');
---

<section id="download" class="download-section">
  <div class="container">
    <h2>Get Started with Caro</h2>
    <p class="section-subtitle">Bring your loyal shell companion to your terminal</p>

    <div class="platform-tabs">
      <button class="tab-button active" data-platform="unix">macOS / Linux</button>
      <button class="tab-button" data-platform="windows">Windows</button>
    </div>

    <div class="install-command" data-platform="unix">
      <code>bash &lt;(curl --proto '=https' --tlsv1.2 -sSfL https://setup.caro.sh)</code>
      <button class="copy-button" data-command="bash <(curl --proto '=https' --tlsv1.2 -sSfL https://setup.caro.sh)" data-copy-label={copyLabel} data-copied-label={copiedLabel}>{copyLabel}</button>
    </div>

    <div class="install-command hidden" data-platform="windows">
      <code>irm https://raw.githubusercontent.com/wildcard/caro/main/install.ps1 | iex</code>
      <button class="copy-button" data-command="irm https://raw.githubusercontent.com/wildcard/caro/main/install.ps1 | iex" data-copy-label={copyLabel} data-copied-label={copiedLabel}>{copyLabel}</button>
    </div>

    <p class="platform-note" data-platform="windows" style="display: none;">Installs to ~/.local/bin and automatically adds to PATH.</p>

    <p class="binary-intro">Or download pre-built binaries (v{SITE_CONFIG.version}):</p>

    <div class="download-options">
      <a href={`${SITE_CONFIG.downloads.baseUrl}/v${SITE_CONFIG.version}/caro-${SITE_CONFIG.version}-macos-silicon`} class="download-link">macOS (Apple Silicon)</a>
      <a href={`${SITE_CONFIG.downloads.baseUrl}/v${SITE_CONFIG.version}/caro-${SITE_CONFIG.version}-macos-intel`} class="download-link">macOS (Intel)</a>
      <a href={`${SITE_CONFIG.downloads.baseUrl}/v${SITE_CONFIG.version}/caro-${SITE_CONFIG.version}-linux-amd64`} class="download-link">Linux (x86_64)</a>
      <a href={`${SITE_CONFIG.downloads.baseUrl}/v${SITE_CONFIG.version}/caro-${SITE_CONFIG.version}-linux-arm64`} class="download-link">Linux (ARM64)</a>
      <a href={`${SITE_CONFIG.downloads.baseUrl}/v${SITE_CONFIG.version}/caro-${SITE_CONFIG.version}-windows-amd64.exe`} class="download-link">Windows</a>
    </div>

    <div class="usage-modes">
      <h3>Multiple Ways to Use Caro</h3>

      <div class="modes-grid">
        <div class="mode-card">
          <h4>🔧 Standalone CLI</h4>
          <div class="mode-command">
            caro "list files &gt; 100MB"
          </div>
        </div>

        <div class="mode-card">
          <h4>🔌 MCP for Claude <span class="coming-soon">Coming Soon</span></h4>
          <p>
            Add Caro as an MCP server to Claude Desktop and let her handle all shell commands seamlessly.
          </p>
        </div>

        <div class="mode-card">
          <h4>✨ Dedicated Skill <span class="available-now">Available Now</span></h4>
          <p>
            Use Caro as a Skill to offload shell command generation and execution while Claude focuses on your work. Install with <code>/plugin install wildcard/caro</code>
          </p>
        </div>
      </div>

      <div class="quick-start">
        <h3>Quick Start</h3>
        <div class="quick-start-steps">
          <p>After running the setup script above, just use Caro:</p>
          <div class="quick-start-command">
            caro "find all python files modified in the last 7 days"
          </div>
          <p>Caro will generate the command and keep you safe. The setup script handles all prerequisites including Rust compilation.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .download-section {
    background: linear-gradient(135deg, #ff8c42 0%, #ff6b35 100%);
    color: white;
    text-align: center;
  }

  .platform-tabs {
    display: flex;
    justify-content: center;
    gap: 8px;
    margin-bottom: 20px;
  }

  .tab-button {
    background: rgba(255, 255, 255, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: white;
    padding: 8px 20px;
    border-radius: 20px;
    cursor: pointer;
    font-size: 14px;
    font-weight: 500;
    transition: all 0.2s;
  }

  .tab-button:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  .tab-button.active {
    background: white;
    color: #ff6b35;
    border-color: white;
  }

  .install-command.hidden {
    display: none;
  }

  .platform-note {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.9);
    margin-top: 12px;
  }

  .platform-note a {
    color: white;
    text-decoration: underline;
  }

  .download-section h2 {
    color: white;
  }

  .download-section .section-subtitle {
    color: rgba(255, 255, 255, 0.9);
  }

  .install-command {
    background: rgba(0, 0, 0, 0.3);
    padding: 20px 30px;
    border-radius: 8px;
    font-family: 'Monaco', 'Courier New', monospace;
    font-size: 16px;
    margin: 30px auto;
    max-width: 600px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .install-command code {
    color: #fff;
  }

  .copy-button {
    background: rgba(255, 255, 255, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: white;
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    font-size: 14px;
    transition: background 0.2s;
  }

  .copy-button:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  .binary-intro {
    margin: 20px 0;
    text-align: center;
  }

  .download-options {
    margin-top: 40px;
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .download-link {
    background: white;
    color: #ff6b35;
    padding: 14px 30px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: transform 0.2s;
  }

  .download-link:hover {
    transform: translateY(-2px);
  }

  .download-link.disabled {
    background: rgba(255, 255, 255, 0.3);
    color: rgba(255, 255, 255, 0.6);
    cursor: not-allowed;
    opacity: 0.6;
  }

  .download-link.disabled:hover {
    transform: none;
  }

  .coming-soon-inline {
    font-size: 14px;
    background: rgba(255, 255, 255, 0.3);
    padding: 4px 10px;
    border-radius: 12px;
    font-weight: 500;
    white-space: nowrap;
  }

  .usage-modes {
    margin-top: 60px;
    padding-top: 40px;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .usage-modes h3 {
    margin-bottom: 30px;
    font-size: 28px;
  }

  .modes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 30px;
    margin-top: 30px;
  }

  .mode-card {
    background: rgba(255, 255, 255, 0.1);
    padding: 25px;
    border-radius: 8px;
  }

  .mode-card h4 {
    margin-bottom: 15px;
    font-size: 20px;
  }

  .mode-card p {
    font-size: 15px;
    line-height: 1.6;
  }

  .mode-command {
    background: rgba(0, 0, 0, 0.3);
    padding: 12px;
    border-radius: 6px;
    margin: 15px 0;
    font-family: monospace;
    font-size: 14px;
    text-align: left;
  }

  .quick-start {
    margin-top: 50px;
    text-align: left;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .quick-start h3 {
    margin-bottom: 20px;
    text-align: center;
  }

  .quick-start-steps {
    font-size: 16px;
    line-height: 1.8;
  }

  .quick-start-steps p {
    margin-bottom: 15px;
  }

  .quick-start-command {
    background: rgba(0, 0, 0, 0.3);
    padding: 15px;
    border-radius: 6px;
    margin: 15px 0;
    font-family: monospace;
  }

  .coming-soon {
    font-size: 12px;
    background: rgba(255, 255, 255, 0.3);
    padding: 3px 8px;
    border-radius: 12px;
    font-weight: 500;
    margin-left: 8px;
    white-space: nowrap;
  }

  .available-now {
    font-size: 12px;
    background: rgba(76, 175, 80, 0.2);
    color: rgba(255, 255, 255, 0.95);
    padding: 3px 8px;
    border-radius: 12px;
    font-weight: 500;
    margin-left: 8px;
    white-space: nowrap;
  }

  .mode-card code {
    background: rgba(0, 0, 0, 0.3);
    padding: 2px 6px;
    border-radius: 4px;
    font-family: 'Monaco', 'Courier New', monospace;
    font-size: 0.9em;
    color: rgba(255, 255, 255, 0.95);
    white-space: nowrap;
  }

  /* Tablet breakpoint */
  @media (max-width: 768px) {
    .download-section h2 {
      font-size: 28px;
    }

    .usage-modes h3 {
      font-size: 24px;
    }

    .install-command {
      padding: 16px 20px;
      font-size: 14px;
    }

    .mode-card {
      padding: 20px;
    }

    .mode-card h4 {
      font-size: 18px;
    }
  }

  /* Mobile breakpoint */
  @media (max-width: 600px) {
    .download-section {
      text-align: left;
    }

    .download-section h2,
    .usage-modes h3,
    .quick-start h3 {
      text-align: center;
    }

    .section-subtitle {
      text-align: center;
    }

    .install-command {
      flex-direction: column;
      gap: 12px;
      text-align: left;
      padding: 16px;
      max-width: 100%;
    }

    .install-command code {
      font-size: 12px;
      word-break: break-all;
      line-height: 1.4;
    }

    .copy-button {
      width: 100%;
      padding: 12px 16px;
      font-size: 15px;
    }

    .download-options {
      flex-direction: column;
      gap: 12px;
      margin-top: 30px;
    }

    .download-link {
      width: 100%;
      text-align: center;
      padding: 14px 20px;
    }

    .modes-grid {
      gap: 16px;
    }

    .mode-card {
      padding: 16px;
    }

    .mode-card h4 {
      font-size: 16px;
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 8px;
    }

    .mode-card p {
      font-size: 14px;
    }

    .quick-start {
      max-width: 100%;
      padding: 0;
    }

    .quick-start-steps {
      font-size: 14px;
    }

    .usage-modes {
      margin-top: 40px;
      padding-top: 30px;
    }

    .coming-soon,
    .available-now {
      font-size: 11px;
      padding: 2px 6px;
    }
  }

  /* Small mobile */
  @media (max-width: 360px) {
    .download-section h2 {
      font-size: 24px;
    }

    .install-command code {
      font-size: 11px;
    }

    .mode-card {
      padding: 14px;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Copy button handling
    const copyButtons = document.querySelectorAll('.copy-button');
    copyButtons.forEach(button => {
      button.addEventListener('click', () => {
        const command = button.getAttribute('data-command') || '';
        const copyLabel = button.getAttribute('data-copy-label') || 'Copy';
        const copiedLabel = button.getAttribute('data-copied-label') || 'Copied!';
        navigator.clipboard.writeText(command).then(() => {
          button.textContent = copiedLabel;
          setTimeout(() => {
            button.textContent = copyLabel;
          }, 2000);
        });
      });
    });

    // Platform tab handling
    const tabButtons = document.querySelectorAll('.tab-button');
    const installCommands = document.querySelectorAll('.install-command[data-platform]');
    const platformNotes = document.querySelectorAll('.platform-note[data-platform]');

    tabButtons.forEach(button => {
      button.addEventListener('click', () => {
        const platform = button.getAttribute('data-platform');

        // Update active tab
        tabButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');

        // Show/hide install commands
        installCommands.forEach(cmd => {
          if (cmd.getAttribute('data-platform') === platform) {
            cmd.classList.remove('hidden');
          } else {
            cmd.classList.add('hidden');
          }
        });

        // Show/hide platform notes
        platformNotes.forEach(note => {
          if (note.getAttribute('data-platform') === platform) {
            note.style.display = 'block';
          } else {
            note.style.display = 'none';
          }
        });
      });
    });
  });
</script>
