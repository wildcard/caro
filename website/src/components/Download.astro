---
---

<section id="download" class="download-section">
  <div class="container">
    <div class="download-header">
╔════════════════════════════════════════════════════════════════════════════╗
║                      [ GET STARTED WITH CARO ]                             ║
╠════════════════════════════════════════════════════════════════════════════╣
║              BRING YOUR LOYAL SHELL COMPANION TO YOUR TERMINAL             ║
╚════════════════════════════════════════════════════════════════════════════╝
    </div>

    <div class="install-command">
      <code>bash &lt;(curl --proto '=https' --tlsv1.2 -sSfL https://setup.caro.sh)</code>
      <button class="copy-button" data-command="bash <(curl --proto '=https' --tlsv1.2 -sSfL https://setup.caro.sh)">Copy</button>
    </div>

    <p style="margin: 20px 0;">Or download pre-built binaries: <span class="coming-soon-inline">Coming Soon</span></p>

    <div class="download-options">
      <span class="download-link disabled">macOS (Apple Silicon)</span>
      <span class="download-link disabled">macOS (Intel)</span>
      <span class="download-link disabled">Linux (x86_64)</span>
      <span class="download-link disabled">Windows</span>
    </div>

    <div class="usage-modes">
      <h3>Multiple Ways to Use Caro</h3>

      <div class="modes-grid">
        <div class="mode-card">
          <h4>🔧 Standalone CLI</h4>
          <div class="mode-command">
            caro "list files &gt; 100MB"
          </div>
        </div>

        <div class="mode-card">
          <h4>🔌 MCP for Claude <span class="coming-soon">Coming Soon</span></h4>
          <p>
            Add Caro as an MCP server to Claude Desktop and let her handle all shell commands seamlessly.
          </p>
        </div>

        <div class="mode-card">
          <h4>✨ Dedicated Skill <span class="coming-soon">Coming Soon</span></h4>
          <p>
            Use Caro as a Skill to offload shell command generation and execution while Claude focuses on your work.
          </p>
        </div>
      </div>

      <div class="quick-start">
        <h3>Quick Start</h3>
        <div class="quick-start-steps">
          <p>After running the setup script above, just use Caro:</p>
          <div class="quick-start-command">
            caro "find all python files modified in the last 7 days"
          </div>
          <p>Caro will generate the command and keep you safe. The setup script handles all prerequisites including Rust compilation.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .download-section {
    background: var(--color-bg-secondary);
    color: var(--color-text);
    text-align: center;
    border-top: 3px solid var(--color-border);
    border-bottom: 3px solid var(--color-border);
    font-family: 'JetBrains Mono', monospace;
    position: relative;
    box-shadow: inset 0 0 60px var(--glow-color);
  }

  .download-header {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    line-height: 1.3;
    white-space: pre;
    color: var(--color-text);
    margin-bottom: 40px;
    font-weight: 700;
    text-shadow: 0 0 10px var(--glow-color);
  }

  .install-command {
    background: var(--color-bg);
    padding: 25px 30px;
    border: 2px solid var(--color-border);
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    margin: 40px auto;
    max-width: 700px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;
    box-shadow: 0 0 30px var(--glow-color), 4px 4px 0 var(--color-border);
  }

  .install-command code {
    color: var(--color-success);
    font-weight: 700;
    text-shadow: 0 0 8px var(--color-success);
    flex: 1;
    word-break: break-all;
  }

  .copy-button {
    background: var(--color-primary);
    border: 2px solid var(--color-border);
    color: var(--color-bg);
    padding: 12px 22px;
    cursor: pointer;
    font-size: 13px;
    font-weight: 700;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 1px;
    box-shadow: 0 0 15px var(--glow-color), 3px 3px 0 var(--color-border);
    transition: all 0.15s ease;
  }

  .copy-button:hover {
    box-shadow: 0 0 25px var(--glow-color), 3px 3px 0 var(--color-border);
    transform: translateY(-2px);
  }

  .copy-button:active {
    box-shadow: 0 0 15px var(--glow-color), 1px 1px 0 var(--color-border);
    transform: translate(2px, 2px);
  }

  .download-options {
    margin-top: 40px;
    display: flex;
    gap: 15px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .download-link {
    background: #000;
    color: var(--color-terminal-green);
    padding: 16px 32px;
    text-decoration: none;
    font-weight: 700;
    border: 4px solid var(--color-border);
    box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.3);
    font-family: 'Unifont', monospace;
    font-size: 14px;
    letter-spacing: 1px;
    transition: none;
  }

  .download-link:hover {
    background: var(--color-terminal-green);
    color: #000;
  }

  .download-link.disabled {
    background: rgba(0, 0, 0, 0.5);
    color: rgba(0, 0, 0, 0.6);
    cursor: not-allowed;
    opacity: 0.5;
  }

  .download-link.disabled:hover {
    background: rgba(0, 0, 0, 0.5);
    color: rgba(0, 0, 0, 0.6);
  }

  .coming-soon-inline {
    font-size: 13px;
    background: #000;
    color: var(--color-terminal-amber);
    padding: 6px 14px;
    font-weight: 700;
    border: 3px solid var(--color-border);
    white-space: nowrap;
    font-family: 'Unifont', monospace;
    letter-spacing: 1px;
  }

  .usage-modes {
    margin-top: 60px;
    padding-top: 40px;
    border-top: 4px solid rgba(0, 0, 0, 0.3);
  }

  .usage-modes h3 {
    margin-bottom: 30px;
    font-size: 24px;
    font-weight: 700;
    letter-spacing: 2px;
  }

  .modes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 25px;
    margin-top: 30px;
  }

  .mode-card {
    background: #000;
    color: #fff;
    padding: 25px;
    border: 4px solid var(--color-border);
    box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.3);
  }

  .mode-card h4 {
    margin-bottom: 15px;
    font-size: 18px;
    font-weight: 700;
    letter-spacing: 1px;
  }

  .mode-card p {
    font-size: 14px;
    line-height: 1.7;
  }

  .mode-command {
    background: rgba(51, 255, 0, 0.1);
    color: var(--color-terminal-green);
    padding: 12px;
    margin: 15px 0;
    font-family: 'Unifont', monospace;
    font-size: 13px;
    text-align: left;
    border: 2px solid var(--color-terminal-green);
  }

  .quick-start {
    margin-top: 50px;
    text-align: left;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    background: #000;
    color: #fff;
    padding: 30px;
    border: 4px solid var(--color-border);
    box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.3);
  }

  .quick-start h3 {
    margin-bottom: 20px;
    text-align: center;
    font-size: 22px;
    letter-spacing: 2px;
  }

  .quick-start-steps {
    font-size: 15px;
    line-height: 1.8;
  }

  .quick-start-steps p {
    margin-bottom: 15px;
  }

  .quick-start-command {
    background: rgba(51, 255, 0, 0.1);
    color: var(--color-terminal-green);
    padding: 15px;
    margin: 15px 0;
    font-family: 'Unifont', monospace;
    border: 2px solid var(--color-terminal-green);
    font-weight: 700;
  }

  .coming-soon {
    font-size: 11px;
    background: rgba(255, 180, 0, 0.3);
    color: var(--color-terminal-amber);
    padding: 4px 10px;
    border: 2px solid var(--color-terminal-amber);
    font-weight: 700;
    margin-left: 8px;
    white-space: nowrap;
    font-family: 'Unifont', monospace;
  }

  @media (max-width: 768px) {
    .download-header {
      font-size: 8px;
      overflow-x: auto;
    }

    .install-command {
      flex-direction: column;
      padding: 20px;
    }

    .modes-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const copyButtons = document.querySelectorAll('.copy-button');
    copyButtons.forEach(button => {
      button.addEventListener('click', () => {
        const command = button.getAttribute('data-command') || '';
        navigator.clipboard.writeText(command).then(() => {
          const originalText = button.textContent;
          button.textContent = 'Copied!';
          setTimeout(() => {
            button.textContent = originalText;
          }, 2000);
        });
      });
    });
  });
</script>
