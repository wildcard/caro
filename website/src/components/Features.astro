---
const features = [
  {
    icon: "shield-check",
    title: "Safety Guardian",
    description: "Comprehensive validation blocks dangerous commands like rm -rf /, fork bombs, and destructive operations. 52 pre-compiled safety patterns with risk-level assessment.",
    status: "implemented",
    statusLabel: "Available Now"
  },
  {
    icon: "globe",
    title: "Cross-Platform Expert",
    description: "Works across macOS, Linux, Windows, GNU, and BSD. Understands platform-specific nuances and respects your distribution of choice.",
    status: "implemented",
    statusLabel: "Available Now"
  },
  {
    icon: "cpu",
    title: "Platform-Aware",
    description: "Provides recommendations based on your platform capabilities and best practices. Distinguishes between BSD and GNU command syntax automatically.",
    status: "implemented",
    statusLabel: "Available Now"
  },
  {
    icon: "check-circle",
    title: "POSIX Specialist",
    description: "Expert in POSIX-compliant shell commands that work reliably across systems. Portable, safe, and optimized for your terminal.",
    status: "implemented",
    statusLabel: "Available Now"
  },
  {
    icon: "zap",
    title: "Lightning Fast",
    description: "Target: Sub-100ms startup, sub-2s inference on Apple Silicon. MLX framework integration for GPU acceleration on M-series chips.",
    status: "in-progress",
    statusLabel: "In Development"
  },
  {
    icon: "heart-handshake",
    title: "Claude's Companion",
    description: "Vision: Seamless integration with Claude as an MCP server and Skill, offloading shell command inference while Claude focuses on your broader work.",
    status: "planned",
    statusLabel: "Planned"
  }
];
---

<section id="features">
  <div class="container">
    <div class="section-header">
╔══════════════════════════════════════════════════════════════════════════╗
║                            [ WHY CARO? ]                                 ║
╠══════════════════════════════════════════════════════════════════════════╣
║          COMPANION AGENT • SAFETY • EMPATHY • EXPERTISE                  ║
╚══════════════════════════════════════════════════════════════════════════╝
    </div>
    <div class="alpha-notice">
      <i data-lucide="construction" class="notice-icon"></i>
      <span class="notice-text">SOFT LAUNCH ALPHA — BUILDING WITH COMMUNITY</span>
    </div>
    <div class="features">
      {features.map((feature) => (
        <div class={`feature feature-${feature.status}`}>
          <div class="feature-header">
            <div class="feature-icon">
              <i data-lucide={feature.icon}></i>
            </div>
            <span
              class={`status-badge status-${feature.status}`}
              role="status"
              aria-label={`Status: ${feature.statusLabel}`}
            >
              {feature.statusLabel}
            </span>
          </div>
          <h3>{feature.title}</h3>
          <p>{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .section-header {
    font-family: 'Unifont', monospace;
    font-size: 11px;
    line-height: 1.3;
    white-space: pre;
    color: var(--color-text);
    margin-bottom: 40px;
    text-align: center;
    font-weight: 700;
    text-shadow: 2px 2px 0 var(--color-orange);
  }

  .alpha-notice {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    text-align: center;
    margin: 20px auto 50px;
    padding: 18px 32px;
    background: var(--color-bg-tertiary);
    border: 2px solid var(--color-warning);
    color: var(--color-text);
    max-width: 650px;
    font-size: 13px;
    line-height: 1;
    font-weight: 700;
    box-shadow: 0 0 20px var(--glow-color), 4px 4px 0 var(--color-warning);
    letter-spacing: 1.5px;
  }

  .notice-icon {
    width: 20px;
    height: 20px;
    color: var(--color-warning);
  }

  .notice-text {
    font-size: 13px;
  }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
    margin-top: 40px;
  }

  .feature {
    text-align: center;
    padding: 0;
    background: var(--color-bg-secondary);
    border: 4px solid var(--color-border);
    box-shadow: 6px 6px 0 var(--color-border);
    transition: none;
    font-family: 'Unifont', monospace;
  }

  .feature:hover {
    box-shadow: 8px 8px 0 var(--color-orange);
    border-color: var(--color-orange);
  }

  .feature-header {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    padding: 20px;
    background: var(--color-bg-tertiary);
    border-bottom: 4px solid var(--color-border);
  }

  .feature-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
  }

  .feature-icon i {
    width: 48px;
    height: 48px;
    filter: drop-shadow(0 0 8px var(--glow-color));
  }

  .status-badge {
    display: inline-block;
    padding: 6px 16px;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    border: 3px solid;
    font-family: 'Unifont', monospace;
    box-shadow: 3px 3px 0 rgba(0, 0, 0, 0.2);
  }

  .status-implemented {
    background: #22c55e;
    color: #000;
    border-color: #15803d;
  }

  :global(.dark) .status-implemented {
    background: #22c55e;
    color: #000;
  }

  .status-in-progress {
    background: #3b82f6;
    color: #000;
    border-color: #1e40af;
  }

  :global(.dark) .status-in-progress {
    background: #3b82f6;
    color: #000;
  }

  .status-planned {
    background: #a855f7;
    color: #000;
    border-color: #6b21a8;
  }

  :global(.dark) .status-planned {
    background: #a855f7;
    color: #000;
  }

  .feature-planned {
    opacity: 0.9;
  }

  .feature-planned:hover {
    opacity: 1;
  }

  .feature h3 {
    font-size: 20px;
    margin-bottom: 16px;
    padding: 0 20px;
    color: var(--color-text);
    font-family: 'Unifont', monospace;
    font-weight: 700;
    letter-spacing: 1px;
  }

  .feature p {
    color: var(--color-text-secondary);
    line-height: 1.8;
    padding: 0 20px 20px;
    font-family: 'Unifont', monospace;
    font-size: 14px;
  }

  @media (max-width: 768px) {
    .features {
      grid-template-columns: 1fr;
    }

    .section-header {
      font-size: 8px;
      overflow-x: auto;
    }

    .feature-icon {
      font-size: 48px;
    }

    .feature h3 {
      font-size: 16px;
    }

    .feature p {
      font-size: 13px;
    }
  }
</style>
