---
---

<div class="container">
  <div class="terminal">
    <div class="terminal-border-top">
╔════════════════════════════════════════════════════════════════════╗
║ GNU/POSIX TERMINAL v1.0 - caro.sh                        [■][□][×] ║
╠════════════════════════════════════════════════════════════════════╣
    </div>
    <div class="terminal-content">
      <div class="terminal-line">
        <span class="terminal-prompt">user@system:~$</span>
        <span class="terminal-command"> caro "find python files modified in the last 7 days"</span>
      </div>
      <div class="terminal-line">
        <span class="terminal-caro">🐕 [CARO]:</span>
        <span class="terminal-output"> find . -name "*.py" -type f -mtime -7</span>
      </div>
      <div class="terminal-line">
        <span class="terminal-safe">✓ [SAFE] Compatible with your macOS system</span>
      </div>
    </div>
    <div class="terminal-border-bottom">
╚════════════════════════════════════════════════════════════════════╝
    </div>
  </div>
</div>

<style>
  .terminal {
    background: #0f0f0f;
    border: 6px double var(--color-terminal-green);
    padding: 0;
    margin: 60px auto;
    max-width: 800px;
    font-family: 'Unifont', 'Courier New', monospace;
    font-size: 14px;
    color: var(--color-terminal-green);
    box-shadow:
      0 0 20px rgba(51, 255, 0, 0.3),
      inset 0 0 40px rgba(0, 0, 0, 0.5),
      8px 8px 0 var(--color-border);
    position: relative;
  }

  .terminal-border-top,
  .terminal-border-bottom {
    font-family: 'Unifont', monospace;
    font-size: 11px;
    line-height: 1.3;
    white-space: pre;
    color: var(--color-terminal-green);
    background: #000;
    padding: 4px;
    text-shadow: 0 0 5px var(--color-terminal-green);
  }

  .terminal-content {
    padding: 20px;
    background: #0a0a0a;
  }

  .terminal-line {
    margin: 12px 0;
    line-height: 1.8;
    font-family: 'Unifont', monospace;
  }

  .terminal-prompt {
    color: var(--color-terminal-amber);
    font-weight: 700;
    text-shadow: 0 0 5px var(--color-terminal-amber);
  }

  .terminal-command {
    color: #fff;
    font-weight: 400;
  }

  .terminal-output {
    color: var(--color-terminal-green);
    font-weight: 700;
    text-shadow: 0 0 3px var(--color-terminal-green);
  }

  .terminal-caro {
    color: var(--color-orange);
    font-weight: 700;
    text-shadow: 0 0 5px var(--color-orange);
  }

  .terminal-safe {
    color: var(--color-terminal-green);
    font-weight: 700;
    text-shadow: 0 0 5px var(--color-terminal-green);
  }

  /* Retro CRT flicker effect */
  @keyframes flicker {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.97; }
  }

  .terminal {
    animation: flicker 0.15s infinite;
  }

  @media (max-width: 768px) {
    .terminal {
      margin: 40px auto;
      font-size: 12px;
    }

    .terminal-border-top,
    .terminal-border-bottom {
      font-size: 8px;
      overflow-x: auto;
    }

    .terminal-content {
      padding: 15px;
    }
  }
</style>
