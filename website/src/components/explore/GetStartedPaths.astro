---
const paths = [
  {
    icon: "👨‍💻",
    title: "Try Caro Today",
    description: "Get started with the core CLI tool - install via cargo and start generating safe commands",
    steps: [
      "Install with: cargo install cmdai",
      "Try: cmdai 'list all files larger than 1GB'",
      "Review the generated command with safety validation",
      "Execute or customize as needed"
    ],
    cta: { text: "Installation Guide", link: "#platform-guides" }
  },
  {
    icon: "🍎",
    title: "Apple Silicon Users",
    description: "Early access to MLX-optimized inference for blazing-fast local command generation",
    steps: [
      "Build from source with MLX support (in development)",
      "Experience sub-2s inference on M1/M2/M3",
      "Run completely offline with local models",
      "Help us test and improve performance"
    ],
    cta: { text: "MLX Details", link: "#backend-options" },
    badge: "In Development"
  },
  {
    icon: "🤖",
    title: "Claude Integration",
    description: "Future MCP and Skill modes will let Claude delegate shell commands to Caro automatically",
    steps: [
      "MCP server integration (planned)",
      "Dedicated Skill mode (planned)",
      "Automatic safety validation for Claude's workflows",
      "Stay tuned for updates"
    ],
    cta: { text: "Integration Roadmap", link: "#integration-methods" },
    badge: "Coming Soon"
  },
  {
    icon: "🔧",
    title: "Advanced Setups",
    description: "Connect to remote inference servers or configure custom backends",
    steps: [
      "Use Ollama for local multi-model support",
      "Connect to vLLM servers for team deployments",
      "Configure backends in ~/.config/cmdai/config.toml",
      "Automatic fallback to embedded CPU backend"
    ],
    cta: { text: "Backend Config", link: "#backend-options" }
  }
];
---

<section id="get-started">
  <div class="container">
    <h2>Choose Your Path</h2>
    <p class="section-subtitle">Get started with Caro based on your workflow and goals</p>
    <div class="paths">
      {paths.map((path) => (
        <div class="path-card">
          <div class="path-icon">{path.icon}</div>
          <div class="title-row">
            <h3>{path.title}</h3>
            {path.badge && <span class="path-badge">{path.badge}</span>}
          </div>
          <p class="path-description">{path.description}</p>
          <div class="path-steps">
            {path.steps.map((step, index) => (
              <div class="step">
                <span class="step-number">{index + 1}</span>
                <span class="step-text">{step}</span>
              </div>
            ))}
          </div>
          <a href={path.cta.link} class="cta-button">{path.cta.text} →</a>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .paths {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 30px;
    margin-top: 50px;
  }

  .path-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 30px;
    transition: transform 0.3s, box-shadow 0.3s, background 0.3s ease, border-color 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .path-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 30px rgba(255, 140, 66, 0.15);
    border-color: #ff8c42;
  }

  :global(.dark) .path-card:hover {
    box-shadow: 0 10px 30px rgba(255, 140, 66, 0.25);
  }

  .path-icon {
    font-size: 48px;
    margin-bottom: 20px;
  }

  .title-row {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 15px;
    flex-wrap: wrap;
  }

  .path-card h3 {
    font-size: 22px;
    margin: 0;
    color: var(--color-text);
    transition: color 0.3s ease;
  }

  .path-badge {
    font-size: 11px;
    font-weight: 600;
    padding: 4px 10px;
    border-radius: 4px;
    background: rgba(33, 150, 243, 0.1);
    color: #2196f3;
    white-space: nowrap;
  }

  .path-description {
    color: var(--color-text-secondary);
    margin-bottom: 25px;
    line-height: 1.6;
    transition: color 0.3s ease;
  }

  .path-steps {
    flex-grow: 1;
    margin-bottom: 25px;
  }

  .step {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    margin-bottom: 15px;
  }

  .step:last-child {
    margin-bottom: 0;
  }

  .step-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: linear-gradient(135deg, #ff8c42 0%, #ff6b35 100%);
    color: white;
    font-size: 12px;
    font-weight: bold;
    flex-shrink: 0;
  }

  .step-text {
    color: var(--color-text-tertiary);
    font-size: 14px;
    line-height: 1.5;
    transition: color 0.3s ease;
  }

  .cta-button {
    display: inline-block;
    padding: 12px 24px;
    background: linear-gradient(135deg, #ff8c42 0%, #ff6b35 100%);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    text-align: center;
    transition: transform 0.2s, box-shadow 0.2s;
    align-self: flex-start;
  }

  .cta-button:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 12px rgba(255, 140, 66, 0.3);
  }

  @media (max-width: 768px) {
    .paths {
      grid-template-columns: 1fr;
    }
  }
</style>
