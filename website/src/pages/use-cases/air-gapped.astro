---
/**
 * Use Case: Air-Gapped & Security Engineers
 *
 * Target Persona: Security Engineer in restricted environments
 * Key Jobs: Offline AI assistance, Compliance-ready approval, Team safety
 *
 * Critical differentiator: Caro is the ONLY AI CLI tool that works offline.
 */

import LandingPage from '../../layouts/LandingPage.astro';
import Navigation from '../../components/Navigation.astro';
import LPDownload from '../../components/landing/LPDownload.astro';
import LPFooter from '../../components/landing/LPFooter.astro';

const jobs = [
  {
    id: 'offline-ai',
    title: 'Offline AI Assistance',
    description: 'Get AI-powered command generation where no other AI tool can operate.',
    painLevel: '5/5',
    frequency: 'Daily',
    trigger: 'Working in air-gapped environment',
  },
  {
    id: 'compliance',
    title: 'Compliance-Ready Tool Approval',
    description: 'Single binary, zero telemetry, open source—designed for security audit.',
    painLevel: '4/5',
    frequency: 'Monthly',
    trigger: 'Getting new tool approved for secure environment',
  },
  {
    id: 'team-safety',
    title: 'Organization-Wide Safety',
    description: 'Deploy custom safety patterns across your team without network dependency.',
    painLevel: '4/5',
    frequency: 'Monthly',
    trigger: 'Standardizing tooling in restricted environment',
  },
];

const environments = [
  {
    name: 'SCIFs',
    fullName: 'Sensitive Compartmented Information Facilities',
    icon: '🏛️',
    description: 'Government classified development environments',
    requirements: ['No external network', 'Auditable binary', 'No telemetry'],
  },
  {
    name: 'Financial Trading',
    fullName: 'Trading Floor Systems',
    icon: '📈',
    description: 'Systems where millisecond latency matters',
    requirements: ['Air-gapped networks', 'Audit trails', 'No cloud dependencies'],
  },
  {
    name: 'Healthcare',
    fullName: 'HIPAA Compliant Environments',
    icon: '🏥',
    description: 'Patient data systems with strict compliance',
    requirements: ['No data exfiltration', 'Audit logging', 'Offline operation'],
  },
  {
    name: 'Industrial',
    fullName: 'OT/SCADA Networks',
    icon: '🏭',
    description: 'Critical infrastructure control systems',
    requirements: ['Complete isolation', 'No network calls', 'Deterministic behavior'],
  },
  {
    name: 'Defense Contractors',
    fullName: 'ITAR/EAR Controlled',
    icon: '🛡️',
    description: 'Export-controlled development environments',
    requirements: ['US-person only access', 'No foreign servers', 'Full audit'],
  },
  {
    name: 'Research Labs',
    fullName: 'Isolated Research Networks',
    icon: '🔬',
    description: 'IP-sensitive research environments',
    requirements: ['Data isolation', 'No cloud sync', 'Self-contained'],
  },
];

const offlineScenarios = [
  {
    title: 'Network Is Down',
    context: 'The datacenter network is the problem you\'re debugging.',
    input: 'caro "show all network interfaces and their status"',
    output: 'ip -br addr show && ip -br link show',
    benefit: 'Works when nothing else does—doesn\'t need the network to help you fix the network.',
  },
  {
    title: 'Classified Development',
    context: 'You\'re in a SCIF writing scripts for a classified system.',
    input: 'caro "find all python files that import requests library"',
    output: 'grep -rl "import requests" --include="*.py" .',
    benefit: 'Same AI assistance as your unclassified colleagues, with zero data risk.',
  },
  {
    title: 'Secure Data Center',
    context: 'Physical servers in a cage, no internet connection by design.',
    input: 'caro "check disk health on all drives"',
    output: 'for disk in /dev/sd?; do smartctl -H $disk 2>/dev/null; done',
    benefit: 'The model runs locally—no waiting for cloud timeouts that will never resolve.',
  },
  {
    title: 'Traveling Without Internet',
    context: 'On a plane, in a remote location, or just in a dead zone.',
    input: 'caro "compress this directory for transfer"',
    output: 'tar -czvf archive.tar.gz ./directory',
    benefit: 'Your productivity doesn\'t depend on hotel wifi.',
  },
];

const complianceFeatures = [
  {
    icon: '📦',
    title: 'Single Binary',
    description: 'One file, no dependencies. Copy it to any machine.',
    audit: 'Hash verifiable, no supply chain complexity.',
  },
  {
    icon: '🔇',
    title: 'Zero Telemetry',
    description: 'No analytics, no usage tracking, no phone home—ever.',
    audit: 'Verify with strace: zero network syscalls.',
  },
  {
    icon: '📖',
    title: 'Open Source',
    description: 'AGPL-3.0 licensed. Read every line of code.',
    audit: 'Full source audit available on GitHub.',
  },
  {
    icon: '🔐',
    title: 'Bundled Model',
    description: 'AI model embedded in binary. No download required.',
    audit: 'No external model fetching, no model injection risk.',
  },
  {
    icon: '✅',
    title: 'SHA256 Verified',
    description: 'Every release includes cryptographic checksums.',
    audit: 'Verify binary integrity before deployment.',
  },
  {
    icon: '📋',
    title: 'Audit Logging',
    description: 'Log every command generated and validated.',
    audit: 'Meet compliance requirements with full command history.',
  },
];

const approvalArguments = [
  {
    objection: '"AI tools need internet access"',
    response: 'Caro bundles a quantized Qwen2.5-Coder model. Zero network calls after install. Verify with: strace -e network ./caro',
  },
  {
    objection: '"We can\'t audit closed-source AI"',
    response: 'Caro is 100% open source (AGPL-3.0). Audit every line: github.com/wildcard/caro',
  },
  {
    objection: '"AI tools collect usage data"',
    response: 'Caro has zero telemetry. Not "opt-out"—literally no telemetry code exists. Verify the source.',
  },
  {
    objection: '"We need a software supply chain review"',
    response: 'Single binary, Rust-based, all dependencies audited via cargo-audit. SBOM available.',
  },
  {
    objection: '"What if it makes network calls we don\'t know about?"',
    response: 'Run it under strace -e network. You\'ll see zero network syscalls. We designed it this way.',
  },
  {
    objection: '"It\'s an AI—how can it be deterministic?"',
    response: 'The safety layer is deterministic regex patterns, not AI. The AI generates, the patterns validate.',
  },
];

const installMethods = [
  {
    name: 'USB Transfer',
    steps: [
      'Download caro binary on internet-connected machine',
      'Verify SHA256 checksum',
      'Transfer to USB drive through approved process',
      'Copy to air-gapped machine: chmod +x ./caro',
      'Run immediately—no additional setup needed',
    ],
    icon: '💾',
  },
  {
    name: 'Build from Source',
    steps: [
      'Clone repository to transfer machine',
      'Transfer source code through approved process',
      'Build: cargo build --release',
      'Binary at: target/release/caro',
      'Full audit trail of what you\'re running',
    ],
    icon: '🔧',
  },
  {
    name: 'Internal Mirror',
    steps: [
      'Host binary on internal package mirror',
      'Security team verifies once',
      'All engineers install from trusted internal source',
      'Centralized update management',
    ],
    icon: '🏢',
  },
];
---

<LandingPage
  title="Caro for Air-Gapped & Secure Environments | Offline AI Commands"
  description="The only AI shell assistant for air-gapped environments. Zero network calls, bundled model, single binary. Designed for SCIFs, trading floors, and classified systems."
  slug="use-cases/air-gapped"
  persona="Air-Gapped Security Engineer"
  painPoint="Need AI assistance without security compromise"
>
  <Navigation variant="use-cases" currentPage="air-gapped" />

  <!-- Hero -->
  <section class="hero">
    <div class="container">
      <div class="hero-badge">Air-Gapped & Security</div>
      <h1>AI Where Nothing<br />Else Can Operate</h1>
      <p class="subtitle">
        8 million developers work in restricted environments where cloud AI is impossible.
        Caro is the only AI shell assistant that works with zero network connectivity—
        bundled model, single binary, designed for security audit.
      </p>

      <div class="key-stats">
        <div class="stat">
          <span class="stat-number">0</span>
          <span class="stat-label">Network calls</span>
        </div>
        <div class="stat">
          <span class="stat-number">1</span>
          <span class="stat-label">Binary file</span>
        </div>
        <div class="stat">
          <span class="stat-number">100%</span>
          <span class="stat-label">Open source</span>
        </div>
      </div>

      <div class="cta-buttons">
        <a href="#download" class="cta-button primary">Get the Binary</a>
        <a href="#compliance" class="cta-button secondary">Security Documentation</a>
      </div>
    </div>
  </section>

  <!-- Environments Section -->
  <section class="environments-section">
    <div class="container">
      <h2>Built for These Environments</h2>
      <p class="section-subtitle">Where cloud AI isn't an option</p>

      <div class="environments-grid">
        {environments.map((env) => (
          <div class="env-card">
            <div class="env-icon">{env.icon}</div>
            <h3>{env.name}</h3>
            <p class="env-fullname">{env.fullName}</p>
            <p class="env-desc">{env.description}</p>
            <ul class="env-reqs">
              {env.requirements.map((req) => (
                <li><span class="check">✓</span> {req}</li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Jobs Section -->
  <section class="jobs-section">
    <div class="container">
      <h2>Jobs Caro Does in Secure Environments</h2>
      <p class="section-subtitle">What you hire Caro to do when offline</p>

      <div class="jobs-grid">
        {jobs.map((job) => (
          <a href={`#${job.id}`} class="job-card">
            <div class="job-header">
              <h3>{job.title}</h3>
              <div class="job-meta">
                <span class="pain-badge">Pain: {job.painLevel}</span>
                <span class="freq-badge">{job.frequency}</span>
              </div>
            </div>
            <p>{job.description}</p>
            <p class="job-trigger"><strong>Trigger:</strong> {job.trigger}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Offline Scenarios -->
  <section id="offline-ai" class="scenarios-section">
    <div class="container">
      <h2>Offline AI in Action</h2>
      <p class="section-subtitle">Real scenarios where Caro shines</p>

      <div class="scenarios-list">
        {offlineScenarios.map((scenario) => (
          <div class="scenario-card">
            <h3>{scenario.title}</h3>
            <p class="scenario-context"><strong>Scenario:</strong> {scenario.context}</p>

            <div class="terminal">
              <div class="terminal-header">
                <div class="terminal-dot red"></div>
                <div class="terminal-dot yellow"></div>
                <div class="terminal-dot green"></div>
                <span class="terminal-title">Air-Gapped Terminal</span>
              </div>

              <div class="terminal-line">
                <span class="prompt">$</span>
                <span class="command"> {scenario.input}</span>
              </div>

              <div class="terminal-line">
                <span class="caro-label">Caro:</span>
                <span class="output"> {scenario.output}</span>
              </div>

              <div class="terminal-line offline-indicator">
                <span class="offline-icon">✈️</span>
                <span class="offline-text">Running fully offline - no network required</span>
              </div>
            </div>

            <p class="scenario-benefit"><strong>Why it matters:</strong> {scenario.benefit}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Compliance Features -->
  <section id="compliance" class="compliance-section">
    <div class="container">
      <h2>Designed for Security Audit</h2>
      <p class="section-subtitle">Every feature built with compliance in mind</p>

      <div class="compliance-grid">
        {complianceFeatures.map((feature) => (
          <div class="compliance-card">
            <div class="compliance-icon">{feature.icon}</div>
            <h3>{feature.title}</h3>
            <p class="compliance-desc">{feature.description}</p>
            <p class="compliance-audit"><strong>Audit:</strong> {feature.audit}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Getting Approval -->
  <section class="approval-section">
    <div class="container">
      <h2>Getting Caro Approved</h2>
      <p class="section-subtitle">Answers to common security review questions</p>

      <div class="approval-list">
        {approvalArguments.map((arg) => (
          <div class="approval-card">
            <div class="objection">
              <span class="objection-icon">❓</span>
              <span class="objection-text">{arg.objection}</span>
            </div>
            <div class="response">
              <span class="response-icon">✅</span>
              <span class="response-text">{arg.response}</span>
            </div>
          </div>
        ))}
      </div>

      <div class="approval-docs">
        <h3>Security Documentation</h3>
        <div class="docs-grid">
          <a href="https://github.com/wildcard/caro/blob/main/SECURITY.md" class="doc-link">
            <span class="doc-icon">📄</span>
            <span>Security Policy</span>
          </a>
          <a href="https://github.com/wildcard/caro/blob/main/LICENSE" class="doc-link">
            <span class="doc-icon">📜</span>
            <span>License (AGPL-3.0)</span>
          </a>
          <a href="https://github.com/wildcard/caro" class="doc-link">
            <span class="doc-icon">💻</span>
            <span>Full Source Code</span>
          </a>
          <a href="https://github.com/wildcard/caro/releases" class="doc-link">
            <span class="doc-icon">📦</span>
            <span>Releases + Checksums</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Installation Methods -->
  <section class="install-section">
    <div class="container">
      <h2>Deployment Options</h2>
      <p class="section-subtitle">How to get Caro into your secure environment</p>

      <div class="install-grid">
        {installMethods.map((method) => (
          <div class="install-card">
            <div class="install-icon">{method.icon}</div>
            <h3>{method.name}</h3>
            <ol class="install-steps">
              {method.steps.map((step) => (
                <li>{step}</li>
              ))}
            </ol>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Verification -->
  <section class="verify-section">
    <div class="container">
      <h2>Verify It Yourself</h2>
      <p class="section-subtitle">Don't trust us—verify these claims</p>

      <div class="verify-grid">
        <div class="verify-card">
          <h3>Verify Zero Network Calls</h3>
          <div class="verify-code">
            <code>strace -e network ./caro "list files"</code>
          </div>
          <p>Run under strace. You'll see zero network-related syscalls.</p>
        </div>

        <div class="verify-card">
          <h3>Verify Binary Integrity</h3>
          <div class="verify-code">
            <code>sha256sum caro && cat caro.sha256</code>
          </div>
          <p>Compare against published checksums on GitHub releases.</p>
        </div>

        <div class="verify-card">
          <h3>Verify No DNS Lookups</h3>
          <div class="verify-code">
            <code>ltrace -e '*dns*' ./caro "list files"</code>
          </div>
          <p>No DNS resolution functions called.</p>
        </div>

        <div class="verify-card">
          <h3>Verify Open Ports</h3>
          <div class="verify-code">
            <code>ss -tlnp | grep caro</code>
          </div>
          <p>Caro opens no listening sockets.</p>
        </div>
      </div>
    </div>
  </section>

  <LPDownload />
  <LPFooter />
</LandingPage>

<style>
  .hero {
    padding: 100px 20px 80px;
    text-align: center;
    background: linear-gradient(135deg, var(--color-bg-tertiary) 0%, var(--color-bg) 100%);
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .hero-badge {
    display: inline-block;
    background: rgba(139, 92, 246, 0.15);
    border: 1px solid rgba(139, 92, 246, 0.3);
    color: #8b5cf6;
    padding: 8px 20px;
    border-radius: 20px;
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 24px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  h1 {
    font-size: 52px;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 24px;
    color: var(--color-text);
  }

  .subtitle {
    font-size: 20px;
    color: var(--color-text-secondary);
    max-width: 700px;
    margin: 0 auto 32px;
    line-height: 1.7;
  }

  .key-stats {
    display: flex;
    justify-content: center;
    gap: 48px;
    margin-bottom: 40px;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-number {
    font-size: 48px;
    font-weight: 700;
    color: #8b5cf6;
    line-height: 1;
  }

  .stat-label {
    font-size: 14px;
    color: var(--color-text-secondary);
    margin-top: 8px;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-button {
    display: inline-block;
    padding: 16px 40px;
    border-radius: 8px;
    font-size: 18px;
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .cta-button.primary {
    background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
    color: white;
  }

  .cta-button.primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(139, 92, 246, 0.3);
  }

  .cta-button.secondary {
    background: transparent;
    border: 2px solid #8b5cf6;
    color: #8b5cf6;
  }

  /* Environments */
  .environments-section {
    padding: 80px 20px;
    background: var(--color-bg);
  }

  h2 {
    font-size: 36px;
    text-align: center;
    margin-bottom: 12px;
    color: var(--color-text);
  }

  .section-subtitle {
    text-align: center;
    color: var(--color-text-secondary);
    font-size: 18px;
    margin-bottom: 50px;
  }

  .environments-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
  }

  .env-card {
    padding: 28px;
    border-radius: 12px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .env-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .env-icon {
    font-size: 40px;
    margin-bottom: 12px;
  }

  .env-card h3 {
    font-size: 20px;
    color: var(--color-text);
    margin-bottom: 4px;
  }

  .env-fullname {
    font-size: 13px;
    color: #8b5cf6;
    margin-bottom: 8px;
  }

  .env-desc {
    font-size: 14px;
    color: var(--color-text-secondary);
    margin-bottom: 16px;
  }

  .env-reqs {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .env-reqs li {
    font-size: 13px;
    color: var(--color-text-secondary);
    padding: 4px 0;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .env-reqs .check {
    color: #22c55e;
    font-weight: 600;
  }

  /* Jobs Section */
  .jobs-section {
    padding: 80px 20px;
    background: var(--color-bg-secondary);
  }

  .jobs-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 24px;
  }

  .job-card {
    padding: 28px;
    border-radius: 12px;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    text-decoration: none;
    color: var(--color-text);
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .job-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .job-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 12px;
    gap: 12px;
  }

  .job-card h3 {
    font-size: 18px;
    color: var(--color-text);
  }

  .job-meta {
    display: flex;
    flex-direction: column;
    gap: 4px;
    flex-shrink: 0;
  }

  .pain-badge {
    font-size: 11px;
    color: #ef4444;
    font-weight: 600;
  }

  .freq-badge {
    font-size: 11px;
    color: var(--color-text-secondary);
    background: var(--color-bg-secondary);
    padding: 2px 8px;
    border-radius: 4px;
  }

  .job-card p {
    font-size: 14px;
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin-bottom: 12px;
  }

  .job-trigger {
    font-size: 13px;
    padding-top: 12px;
    border-top: 1px solid var(--color-border);
  }

  /* Scenarios */
  .scenarios-section {
    padding: 80px 20px;
    background: var(--color-bg);
  }

  .scenarios-list {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  .scenario-card {
    padding: 32px;
    border-radius: 16px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
  }

  .scenario-card h3 {
    font-size: 22px;
    color: var(--color-text);
    margin-bottom: 12px;
  }

  .scenario-context {
    font-size: 15px;
    color: var(--color-text-secondary);
    margin-bottom: 20px;
  }

  .terminal {
    background: #1a1a2e;
    border-radius: 12px;
    padding: 20px;
    font-family: 'SF Mono', 'Monaco', monospace;
    font-size: 14px;
    margin-bottom: 16px;
  }

  .terminal-header {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .terminal-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .terminal-dot.red { background: #ff5f56; }
  .terminal-dot.yellow { background: #ffbd2e; }
  .terminal-dot.green { background: #27c93f; }

  .terminal-title {
    margin-left: auto;
    font-size: 12px;
    color: #666;
    font-family: system-ui, sans-serif;
  }

  .terminal-line {
    margin: 8px 0;
    color: #d4d4d4;
  }

  .prompt { color: #4ec9b0; }
  .command { color: #9cdcfe; }
  .output { color: #ce9178; }
  .caro-label { color: #ff8c42; font-weight: 600; }

  .offline-indicator {
    margin-top: 16px;
    padding: 10px 14px;
    background: rgba(139, 92, 246, 0.15);
    border: 1px solid rgba(139, 92, 246, 0.3);
    border-radius: 6px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .offline-icon { font-size: 16px; }
  .offline-text { color: #8b5cf6; font-size: 13px; }

  .scenario-benefit {
    font-size: 14px;
    color: var(--color-text-secondary);
    padding: 12px 16px;
    background: var(--color-bg);
    border-radius: 8px;
  }

  /* Compliance */
  .compliance-section {
    padding: 80px 20px;
    background: var(--color-bg-secondary);
  }

  .compliance-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
  }

  .compliance-card {
    padding: 28px;
    border-radius: 12px;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
  }

  .compliance-icon {
    font-size: 36px;
    margin-bottom: 12px;
  }

  .compliance-card h3 {
    font-size: 18px;
    color: var(--color-text);
    margin-bottom: 8px;
  }

  .compliance-desc {
    font-size: 14px;
    color: var(--color-text-secondary);
    margin-bottom: 12px;
  }

  .compliance-audit {
    font-size: 13px;
    color: #8b5cf6;
    padding: 8px 12px;
    background: rgba(139, 92, 246, 0.1);
    border-radius: 6px;
  }

  /* Approval */
  .approval-section {
    padding: 80px 20px;
    background: var(--color-bg);
  }

  .approval-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-bottom: 48px;
  }

  .approval-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    overflow: hidden;
  }

  .objection, .response {
    padding: 20px 24px;
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }

  .objection {
    background: rgba(239, 68, 68, 0.05);
    border-bottom: 1px solid var(--color-border);
  }

  .objection-icon { font-size: 18px; }
  .objection-text {
    font-weight: 600;
    color: var(--color-text);
    font-size: 15px;
  }

  .response {
    background: rgba(34, 197, 94, 0.05);
  }

  .response-icon { font-size: 18px; }
  .response-text {
    color: var(--color-text-secondary);
    font-size: 14px;
    line-height: 1.6;
  }

  .approval-docs {
    text-align: center;
  }

  .approval-docs h3 {
    font-size: 22px;
    margin-bottom: 24px;
    color: var(--color-text);
  }

  .docs-grid {
    display: flex;
    justify-content: center;
    gap: 24px;
    flex-wrap: wrap;
  }

  .doc-link {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 20px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    text-decoration: none;
    color: var(--color-text);
    font-size: 14px;
    transition: border-color 0.2s, transform 0.2s;
  }

  .doc-link:hover {
    border-color: #8b5cf6;
    transform: translateY(-2px);
  }

  .doc-icon { font-size: 18px; }

  /* Install */
  .install-section {
    padding: 80px 20px;
    background: var(--color-bg-secondary);
  }

  .install-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
  }

  .install-card {
    padding: 28px;
    border-radius: 12px;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
  }

  .install-icon {
    font-size: 36px;
    margin-bottom: 12px;
  }

  .install-card h3 {
    font-size: 18px;
    color: var(--color-text);
    margin-bottom: 16px;
  }

  .install-steps {
    padding-left: 20px;
    margin: 0;
  }

  .install-steps li {
    font-size: 14px;
    color: var(--color-text-secondary);
    padding: 6px 0;
    line-height: 1.5;
  }

  /* Verify */
  .verify-section {
    padding: 80px 20px;
    background: var(--color-bg);
  }

  .verify-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
  }

  .verify-card {
    padding: 24px;
    border-radius: 12px;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
  }

  .verify-card h3 {
    font-size: 16px;
    color: var(--color-text);
    margin-bottom: 12px;
  }

  .verify-code {
    background: #1a1a2e;
    padding: 12px 16px;
    border-radius: 8px;
    margin-bottom: 12px;
  }

  .verify-code code {
    color: #9cdcfe;
    font-family: 'SF Mono', monospace;
    font-size: 13px;
  }

  .verify-card p {
    font-size: 13px;
    color: var(--color-text-secondary);
  }

  @media (max-width: 768px) {
    h1 { font-size: 36px; }
    h2 { font-size: 28px; }

    .key-stats {
      flex-direction: column;
      gap: 24px;
    }

    .stat-number {
      font-size: 36px;
    }
  }
</style>
